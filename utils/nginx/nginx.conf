worker_processes auto;
pid /var/run/nginx/nginx.pid;

events {
    worker_connections 1024;
}

http {
    client_body_temp_path /var/cache/nginx/client_temp;
    proxy_temp_path       /var/cache/nginx/proxy_temp;
    fastcgi_temp_path     /var/cache/nginx/fastcgi_temp;
    uwsgi_temp_path       /var/cache/nginx/uwsgi_temp;
    scgi_temp_path        /var/cache/nginx/scgi_temp;

    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    server {
        listen 80;
        server_name localhost; # Replace with your domain name

        # Omni Desk Frontend (served directly from Nginx static files)
        location / {
            root /usr/share/nginx/html; # This path is mounted from omni_desk_frontend/build
            index index.html index.htm;
            try_files $uri $uri/ /index.html;
        }

        # Omni Desk Backend API
        location /api/ {
            proxy_pass http://backend:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Omni Desk Backend Static Files (if any, though frontend should handle most)
        location /static/ {
            proxy_pass http://backend:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        # Omni Desk Backend Admin
        location /admin/ {
            proxy_pass http://backend:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Dify Reverse Proxy
        # Assuming Dify is accessible via /dify path
        location /dify/ {
            rewrite ^/dify/(.*)$ /$1 break; # Remove /dify prefix
            proxy_pass http://dify_app:80; # Proxy to Dify container
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_redirect off; # Important for correct redirects
        }

        # Ragflow Reverse Proxy
        # Assuming Ragflow is accessible via /ragflow path
        location /ragflow/ {
            rewrite ^/ragflow/(.*)$ /$1 break; # Remove /ragflow prefix
            proxy_pass http://ragflow_app:80; # Proxy to Ragflow container
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_redirect off; # Important for correct redirects
        }
    }

    sendfile on;
    keepalive_timeout 65;
}