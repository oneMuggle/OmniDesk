# OmniDesk 离线部署包 v0.5.6 测试方案

> **目标版本:** v0.5.6 (git_sha: 537aa80, 构建时间: 2026-07-01T15:34:43Z)
> **介质路径:** `deployment/docker/omnidesk-offline-v0.5.6/`
> **方案日期:** 2026-07-04
> **状态:** 进行中

## 1. 当前状态快照

| 项目 | 状态 | 备注 |
|---|---|---|
| 部署介质 | ✅ 已就绪 | `omnidesk-offline-v0.5.6/` 5 个 tar + compose + 配置 + 脚本 |
| SHA256 校验 | ✅ 全部通过 | 5/5 镜像 tar + 9 个配置文件 checksums 一致 |
| 镜像已加载 | ✅ v0.5.6 后端/前端镜像已在 `docker images` 中 | backend 149MB / frontend 38MB |
| v0.5.6 容器 | ❌ 未启动 | 当前 `compose_*` 跑的是 v0.5.5 |
| 端口 80 | ⚠️ 被占用 | `infra-nginx` 容器占用 |
| 端口 8082 | ⚠️ 被占用 | 当前 v0.5.5 frontend |
| 端口 8000 | ⚠️ 被占用 | paperless-webserver 占用 |
| 部署脚本 | ✅ deploy.sh / verify.sh / rollback.sh / backup.sh | 含 IMAGE_TAG 自动同步、GHCR retag |

### v0.5.6 修复点对照

| 修复点 | 验证位置 | 状态 |
|---|---|---|
| CORS 空字符串不再触发 `corsheaders.E013` | `omni_desk_backend/omni_desk_backend/settings/production.py:58` | ✅ 已使用列表推导过滤空值 |
| docker-compose 必填变量用 `${VAR:?msg}` 语法 | `compose/docker-compose.offline.yml:21-23,48` | ✅ POSTGRES_* / REDIS_PASSWORD 已用强制语法 |
| nginx 限流注释修正(解释 http context 限制) | `config/nginx.conf:9-11` | ✅ 注释已对齐实际架构 |

## 2. 测试前置条件

### 2.1 环境检查清单

```bash
# [必做] 主机环境核查
docker --version          # 期望: Docker 20.10+ (当前 29.6.1 ✅)
docker compose version    # 期望: v2+ (当前 v5.2.0 ✅)
sha256sum --version       # 校验和工具

# [必做] 系统资源
free -h                   # 期望: 空闲内存 >= 4GB
df -h /var/lib/docker     # 期望: 空闲磁盘 >= 10GB
nproc                     # 期望: CPU >= 2 核

# [必做] 端口可用性
ss -tlnp | grep -E ':(80|8000|5432|6379)\s'   # 检查 80/8000/5432/6379 是否被占
```

### 2.2 数据隔离

| 隔离项 | 措施 |
|---|---|
| 数据卷命名 | 使用独立项目名 `omni_desk_test_056`,避免污染 `compose_*` (v0.5.5) 数据 |
| 网络隔离 | `docker compose -p omni_desk_test_056 ...`,自动生成独立网络 |
| 数据库 | 新建 `postgres_data_test` volume,不复用 `compose_postgres_data` |
| 配置目录 | 在 `/tmp/omni_test_056/` 下生成 `.env.production`,避免污染 `compose/.env.production` |

## 3. 测试流程(11 个阶段,STOP-at-failure)

### 阶段 1:介质完整性校验

```bash
cd /home/fz/project/OmniDesk/deployment/docker/omnidesk-offline-v0.5.6

# 1.1 SHA256 校验全部文件
sha256sum -c CHECKSUMS.sha256
# 期望: 所有行 "OK"

# 1.2 内置 verify.sh(完整流程)
bash scripts/verify.sh
# 期望: "校验通过:所有检查项均通过"

# 1.3 必填文件存在性
for f in images/omni_desk_backend.tar images/omni_desk_frontend.tar \
         images/postgres-14-alpine.tar images/redis-7-alpine.tar \
         images/nginx-stable-alpine.tar scripts/deploy.sh \
         compose/docker-compose.offline.yml config/.env.production.example \
         VERSION BUILD-MANIFEST.json; do
  [ -f "$f" ] && echo "OK: $f" || echo "MISSING: $f"
done
```

**通过标准:** 14 个文件全 OK + verify.sh 返回 0

### 阶段 2:镜像加载与命名同步

```bash
cd /home/fz/project/OmniDesk/deployment/docker/omnidesk-offline-v0.5.6

# 2.1 加载所有镜像
for tar in images/*.tar; do
  docker load -i "$tar"
done

# 2.2 验证源 tag 存在
docker images | grep -E "omni-desk-(backend|frontend)-prod:v0.5.6"
docker images | grep -E "postgres:14-alpine|redis:7-alpine|nginx:stable-alpine"

# 2.3 验证 deploy.sh 的 retag 逻辑
bash scripts/deploy.sh  # 默认 start,会调 load_images()
# 或手动模拟:
docker tag omni-desk-backend-prod:v0.5.6  ghcr.io/onemuggle/omni-desk-backend:v0.5.6
docker tag omni-desk-frontend-prod:v0.5.6 ghcr.io/onemuggle/omni-desk-frontend:v0.5.6

# 2.4 验证 BUILD-MANIFEST.json 的 digest 与 docker 一致
EXPECTED_BACKEND="sha256:2920e0f42d466e867be56fe993b27869a85c6ed7b7ba0fbbfd8f87923af34cd7"
ACTUAL_BACKEND=$(docker image inspect ghcr.io/onemuggle/omni-desk-backend:v0.5.6 --format '{{.Id}}')
[ "$ACTUAL_BACKEND" = "$EXPECTED_BACKEND" ] && echo "PASS: backend digest 一致" || echo "FAIL"

EXPECTED_FRONTEND="sha256:f2416d35e2869f4862735957866d7827be7c01e44d9b5fee69c4a2f1a661b414"
ACTUAL_FRONTEND=$(docker image inspect ghcr.io/onemuggle/omni-desk-frontend:v0.5.6 --format '{{.Id}}')
[ "$ACTUAL_FRONTEND" = "$EXPECTED_FRONTEND" ] && echo "PASS: frontend digest 一致" || echo "FAIL"
```

**通过标准:** 5 个镜像加载成功 + BUILD-MANIFEST digest 与 docker Id 匹配

### 阶段 3:Compose 配置静态校验

```bash
cd /home/fz/project/OmniDesk/deployment/docker/omnidesk-offline-v0.5.6

# 3.1 准备测试 .env(放 /tmp 隔离)
mkdir -p /tmp/omni_test_056
sed 's|<GENERATE-NEW-SECRET-KEY>|test_secret_key_for_validation_only|g;
     s|<CHANGE-TO-STRONG-PASSWORD>|test_password_123|g;
     s|<CHANGE-TO-DB-USER>|omni_test_user|g' \
     config/.env.production.example > /tmp/omni_test_056/.env.production

# 3.2 镜像语法解析(不需要真实环境变量)
docker compose -f compose/docker-compose.offline.yml \
  --env-file /tmp/omni_test_056/.env.production config
# 期望: 输出完整 YAML,无错误
#   - 5 个服务: db / redis / backend / frontend / worker
#   - 4 个 volumes: postgres_data / redis_data / static_volume / media_volume / backup_volume
#   - 1 个网络: omni_desk

# 3.3 必填变量缺失时的行为(应是 docker compose config 直接报错)
sed 's/^POSTGRES_DB=.*/POSTGRES_DB=/' /tmp/omni_test_056/.env.production > /tmp/omni_test_056/.env.empty_db
docker compose -f compose/docker-compose.offline.yml \
  --env-file /tmp/omni_test_056/.env.empty_db config 2>&1 | grep -E "POSTGRES_DB is required"
# 期望: 看到 "POSTGRES_DB is required" 错误(验证 ${VAR:?msg} 修复点)
```

**通过标准:** compose config 无语法错误 + 必填变量缺失时立即报错

### 阶段 4:启动服务(端到端)

```bash
cd /home/fz/project/OmniDesk/deployment/docker/omnidesk-offline-v0.5.6

# 4.1 用独立项目名启动,避免污染现有 compose_*
docker compose -p omni_desk_test_056 \
  -f compose/docker-compose.offline.yml \
  --env-file /tmp/omni_test_056/.env.production up -d

# 4.2 查看容器状态(应在 60-90 秒内全部 healthy)
docker compose -p omni_desk_test_056 ps
# 期望: 5 个服务全 healthy

# 4.3 检查容器日志有无 ERROR / Traceback
docker compose -p omni_desk_test_056 logs backend 2>&1 | grep -iE "error|traceback|exception|improperlyconfigured"
# 期望: 仅看到 gunicorn 启动 info 日志,无配置错误

docker compose -p omni_desk_test_056 logs backend 2>&1 | grep -iE "corsheaders\.E013|E001"
# 期望: 没有任何 E013/E001(验证 CORS 修复点)
```

**通过标准:** 5 服务 healthy + 无 E013 / E001 / ImproperlyConfigured

### 阶段 5:数据库迁移与初始化

```bash
# 5.1 数据库迁移(对应 v0.5.4 引入的 EncryptedCharField max_length=18→64 migration)
docker compose -p omni_desk_test_056 exec -T backend python manage.py migrate
# 期望: 显示所有 migrations 应用 OK,无 "django.db.utils.ProgrammingError"

# 5.2 收集静态文件
docker compose -p omni_desk_test_056 exec -T backend python manage.py collectstatic --noinput
# 期望: 收集成功,无报错

# 5.3 路由同步(对应 v0.5.4 sync_routes 多路径修复)
docker compose -p omni_desk_test_056 exec -T backend python manage.py sync_routes
# 期望: 同步 N 个前端路由(根据 routes.json),无 "FileNotFoundError: routes.json"

# 5.4 验证 routes.json 在 backend 镜像内可被读取
docker compose -p omni_desk_test_056 exec -T backend ls -la /usr/src/app/staticfiles/routes.json
# 期望: 文件存在(对应 Dockerfile COPY 修复)
```

**通过标准:** migrate / collectstatic / sync_routes 全成功

### 阶段 6:健康检查端点

```bash
# 6.1 公开健康端点(无需认证)
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8083/api/health/
# 期望: 200(注意:8083 是因为 80 被占,我们需要临时改端口)

# 6.2 前端首页
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8083/
# 期望: 200,Content-Type: text/html

# 6.3 检查健康端点返回 JSON 结构
curl -s http://localhost:8083/api/health/ | python3 -m json.tool
# 期望: {"status": "healthy", "components": {...}}

# 6.4 /api/system/ready/ (readiness 区分)
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8083/api/system/ready/
# 期望: 200(AllowAny,DB+Redis+Celery 检查)
```

**注意:** 端口冲突时,需先停掉占 80 端口的容器或临时改 compose 端口映射:
```bash
# 方案 A: 临时把 frontend 端口改成 8083(测试完恢复)
# 在 compose/docker-compose.offline.yml 第 111 行加 "8083:80"
```

**通过标准:** 健康端点全部 200

### 阶段 7:认证流程端到端

```bash
# 7.1 注册新用户(对应 v0.5.4 修复的 6 个注册场景)
# 注意:实际端点是 /api/auth/registration/ + 字段 password_confirmation(不是 password2)
curl -s -X POST http://localhost:8083/api/auth/registration/ \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser_056","password":"TestPass123!","password_confirmation":"TestPass123!","email":"t@t.com"}'
# 期望: 201 Created,返回 {"success": true, "user": {...}}

# 7.2 登录获取 JWT
TOKEN=$(curl -s -X POST http://localhost:8083/api/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser_056","password":"TestPass123!"}' | python3 -c "import sys,json;print(json.load(sys.stdin)['access'])")
# 期望: 得到非空 token

# 7.3 访问受保护端点
curl -s -H "Authorization: Bearer $TOKEN" \
  http://localhost:8083/api/system/version/ | python3 -m json.tool
# 期望: 返回 {"version":"0.5.6","build_time":"...","django_version":"4.2.x"}

# 7.4 错误密码
curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:8083/api/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser_056","password":"wrong"}'
# 期望: 401 Unauthorized

# 7.5 无 token 访问受保护资源
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8083/api/system/version/
# 期望: 401 Unauthorized
```

**通过标准:** 注册/登录/鉴权全流程通过

### 阶段 8:核心 API 烟雾测试

```bash
TOKEN=$(curl -s -X POST http://localhost:8083/api/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser_056","password":"TestPass123!"}' | python3 -c "import sys,json;print(json.load(sys.stdin)['access'])")

# 8.1 人员列表
curl -s -H "Authorization: Bearer $TOKEN" \
  http://localhost:8083/api/personnel/ | python3 -c "import sys,json;d=json.load(sys.stdin);print(f'count={d.get(\"count\",len(d))}')"

# 8.2 文档列表
curl -s -H "Authorization: Bearer $TOKEN" \
  http://localhost:8083/api/documents/ | python3 -c "import sys,json;d=json.load(sys.stdin);print(f'count={d.get(\"count\",len(d))}')"

# 8.3 通知未读数
curl -s -H "Authorization: Bearer $TOKEN" \
  http://localhost:8083/api/notifications/unread_count/

# 8.4 路由权限端点(应能列出前端 24 条路由)
# 注意:实际是 /api/permissions/pages/,不是 /api/permissions/routes/
curl -s -H "Authorization: Bearer $TOKEN" \
  http://localhost:8083/api/permissions/pages/ | python3 -c "import sys,json;d=json.load(sys.stdin);print(f'pages={len(d) if isinstance(d,list) else d.get(\"count\",\"?\")}')"

# 8.5 智能助手分流(对应 v0.5.0 多 Agent 协作)
# 注意:实际端点是 /api/smart-assistant/chat/,字段名是 query(不是 message)
curl -s -X POST http://localhost:8083/api/smart-assistant/chat/ \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"query":"你好,请帮我查看今日通知"}'
# 期望: 200,返回 JSON {"answer":"...","intent":"general_chat","conversation_id":N}
#       LLM 端点(localhost:11434)不在离线包内,answer 字段可能含 "回答生成失败" 错误信息
```

**通过标准:** 主要业务 API 全部 200 + 数据正常返回

### 阶段 9:Celery Worker 验证

```bash
# 9.1 Celery 进程状态
docker compose -p omni_desk_test_056 exec -T worker \
  celery -A omni_desk_backend status
# 期望: 看到 "online" 状态的 worker

# 9.2 Worker 日志无 ERROR
docker compose -p omni_desk_test_056 logs worker 2>&1 | grep -iE "error|traceback|connection refused"
# 期望: 无 Redis/DB 连接错误

# 9.3 触发一个异步任务(若 djangoshell 可用)
docker compose -p omni_desk_test_056 exec -T backend python manage.py shell -c "
from omni_desk_backend.celery_app import app
result = app.send_task('omni_desk_backend.celery_app.debug_task')
print('Task ID:', result.id)
print('Result:', result.get(timeout=10))
"
# 期望: 任务成功执行并返回结果
```

**通过标准:** Celery worker 在线 + 能执行任务

### 阶段 10:v0.5.6 修复点专项回归

```bash
# 10.1 【修复点 1】 CORS 空值场景
# 在 .env 中清空 CORS_ALLOWED_ORIGINS,重启 backend
sed 's/^CORS_ALLOWED_ORIGINS=.*/CORS_ALLOWED_ORIGINS=/' \
  /tmp/omni_test_056/.env.production > /tmp/omni_test_056/.env.no_cors

docker compose -p omni_desk_test_056 -f compose/docker-compose.offline.yml \
  --env-file /tmp/omni_test_056/.env.no_cors up -d backend

sleep 10
docker compose -p omni_desk_test_056 logs backend 2>&1 | grep -E "corsheaders|E013|E001"
# 期望: 无 E013/E001 错误,backend 正常启动

# 10.2 【修复点 2】 必填变量缺失场景
# 用没有 POSTGRES_PASSWORD 的 env 启动,期望 docker compose config 直接拒绝
sed '/^POSTGRES_PASSWORD=/d' /tmp/omni_test_056/.env.production > /tmp/omni_test_056/.env.no_pw
docker compose -p omni_desk_test_056 -f compose/docker-compose.offline.yml \
  --env-file /tmp/omni_test_056/.env.no_pw config 2>&1 | head -5
# 期望: 错误信息包含 "POSTGRES_PASSWORD is required"(不是空启动后才 ImproperlyConfigured)

# 10.3 【修复点 3】 nginx 限流注释(静态文档,无需动态测试)
grep -A2 "limit_req_zone" compose/../config/nginx.conf
# 期望: 注释准确说明"必须在 http context 定义,本文件是 server context"
```

**通过标准:** 3 个修复点全部回归通过

### 阶段 11:稳定性与资源

```bash
# 11.1 持续运行 10 分钟,观察容器重启次数
docker compose -p omni_desk_test_056 ps --format json | python3 -c "
import sys, json
for line in sys.stdin:
    data = json.loads(line)
    print(f'{data[\"Service\"]}: restart={data.get(\"RestartCount\",0)} status={data[\"State\"]}')
"
# 期望: RestartCount=0

# 11.2 资源占用
docker stats --no-stream $(docker compose -p omni_desk_test_056 ps -q)
# 期望: backend < 1G / frontend < 128M / worker < 1G / db < 1G / redis < 256M(对齐 compose deploy.resources.limits)

# 11.3 日志体积检查
du -sh /var/lib/docker/containers/$(docker compose -p omni_desk_test_056 ps -q backend)/*-json.log*
# 期望: < 50MB(json-file driver 限制 max-size=50m max-file=3)

# 11.4 模拟错误响应:停止 db 容器,看 backend healthcheck 是否 fail
docker compose -p omni_desk_test_056 stop db
sleep 20
docker compose -p omni_desk_test_056 ps db backend
# 期望: db=Exit 0,backend 变为 unhealthy(不应一直显示 healthy)
docker compose -p omni_desk_test_056 start db
```

**通过标准:** 0 重启 + 资源在限制内 + healthcheck 真实反映状态

## 4. 测试完成后的清理

```bash
# 4.1 停止并删除测试容器、网络、卷(干净退出)
docker compose -p omni_desk_test_056 -f compose/docker-compose.offline.yml \
  --env-file /tmp/omni_test_056/.env.production down -v

# 4.2 清理测试配置
rm -rf /tmp/omni_test_056/

# 4.3 (可选)清理已加载的镜像(节省空间)
docker image rm ghcr.io/onemuggle/omni-desk-backend:v0.5.6 \
                 ghcr.io/onemuggle/omni-desk-frontend:v0.5.6 \
                 omni-desk-backend-prod:v0.5.6 \
                 omni-desk-frontend-prod:v0.5.6

# 4.4 保留现有 v0.5.5 运行不被破坏(本环境关键约束)
docker ps | grep compose-  # 确认 v0.5.5 容器仍在 healthy 状态
```

## 5. 通过/失败判定矩阵

| 阶段 | 通过条件 | 失败处理 |
|---|---|---|
| 1. 介质校验 | 14/14 文件 OK + verify.sh exit 0 | 重新下载介质 |
| 2. 镜像加载 | 5/5 tar 加载成功 + digest 匹配 | 检查 tar 完整性 |
| 3. compose config | YAML 解析无错 + 必填变量报错清晰 | 修复 env 或 compose |
| 4. 容器启动 | 5 服务 healthy + 无配置错误 | 查看 logs 修复 |
| 5. 迁移 | migrate / collectstatic / sync_routes 全成功 | 回滚到上一个能跑迁移的版本 |
| 6. 健康检查 | /api/health/ 与 / 均返回 200 | 检查后端日志 |
| 7. 认证 | 注册/登录/JWT 全流程通过 | 检查 SimpleJWT 配置 |
| 8. API 烟雾 | 主要端点全部 200 + 数据正常 | 业务模块回归 |
| 9. Celery | worker online + 任务可执行 | 检查 Redis 连通 |
| 10. 修复点回归 | 3 个修复点全部重现并修复验证 | 阻塞,需修复 |
| 11. 稳定性 | 0 重启 + 资源达标 + healthcheck 真实 | 调资源限制或 healthcheck 阈值 |

## 6. 已知风险与注意事项

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| 端口 80/8000/8082 已被现有容器占用 | 测试无法直接监听标准端口 | 测试用独立项目名 + 修改 compose 端口映射为 8083 |
| 当前 v0.5.5 compose 在运行 | 误操作可能影响生产 | 所有测试命令必须带 `-p omni_desk_test_056` |
| Docker 29.x 的 compose v5 与 v2 行为差异 | 部分命令语法可能不兼容 | 测试时已确认 v5.2.0,v1/v2 语法都支持 |
| 网络 `compose_omni_desk` 已存在 | 不会冲突,但需注意独立网络隔离 | 测试用项目名隔离 |
| 主机磁盘空间 | 5 个 tar ≈ 350MB + 加载后镜像 ≈ 1GB | 至少预留 10GB 空闲空间 |

## 7. 测试执行清单(checklist 模板)

```
□ 阶段 1:介质完整性校验
  □ SHA256 全 OK
  □ verify.sh exit 0
  □ 14 个必填文件全存在

□ 阶段 2:镜像加载
  □ 5 tar 加载成功
  □ BUILD-MANIFEST digest 与 docker Id 一致
  □ deploy.sh retag 逻辑通过

□ 阶段 3:compose config
  □ YAML 解析无错
  □ 必填变量缺失时立即报错

□ 阶段 4:容器启动
  □ 5 服务 healthy
  □ backend 无 corsheaders.E013/E001
  □ backend 无 ImproperlyConfigured

□ 阶段 5:迁移
  □ migrate 成功
  □ collectstatic 成功
  □ sync_routes 成功(routes.json 可访问)

□ 阶段 6:健康检查
  □ /api/health/ 返回 200
  □ / 返回 200 + text/html

□ 阶段 7:认证
  □ 注册 201
  □ 登录返回 JWT
  □ 受保护端点 携带 token 可访问
  □ 错误密码 401
  □ 无 token 受保护端点 401

□ 阶段 8:API 烟雾
  □ personnel / documents / notifications / routes / assistant 全部 200

□ 阶段 9:Celery
  □ worker online
  □ 任务可执行

□ 阶段 10:v0.5.6 修复点回归
  □ 修复点 1 (CORS): 空 CORS_ALLOWED_ORIGINS 不报错
  □ 修复点 2 (compose): 必填变量缺失立即报错
  □ 修复点 3 (nginx 注释): 注释与实际架构一致

□ 阶段 11:稳定性
  □ 0 容器重启
  □ 资源在 limits 内
  □ healthcheck 真实反映状态

□ 清理
  □ 测试容器已 down -v
  □ 测试 env 已删除
  □ v0.5.5 容器仍在 healthy
```

## 8. 修订记录

### 2026-07-04 第一次执行后修订

| 测试方案原文 | 实际 | 原因 |
|---|---|---|
| `POST /api/users/register/` | `POST /api/auth/registration/` | 项目使用 dj-rest-auth 风格端点 |
| 字段 `password2` | 字段 `password_confirmation` | dj-rest-auth 默认密码确认字段 |
| `GET /api/permissions/routes/` | `GET /api/permissions/pages/` | permissions app 的实际端点 |
| `POST /api/assistant/chat/` + 字段 `message` | `POST /api/smart-assistant/chat/` + 字段 `query` | smart-assistant app 的实际端点 |

实际端点列表可通过进入 backend 容器执行:
```bash
docker compose -p omni_desk_test_056 exec -T backend python manage.py shell -c "
import django; django.setup()
from django.urls import get_resolver
def list_urls(lurl='', patterns=None):
    patterns = patterns or get_resolver().url_patterns
    for p in patterns:
        if hasattr(p, 'url_patterns'):
            list_urls(lurl + str(p.pattern), p.url_patterns)
        else:
            full = lurl + str(p.pattern)
            if 'api/' in full:
                print(full)
list_urls()
" 2>&1 | grep 'api/'
```

## 9. 关键参考

- 部署脚本源码: `deployment/docker/omnidesk-offline-v0.5.6/scripts/`
- Compose 配置: `compose/docker-compose.offline.yml`
- 环境变量模板: `config/.env.production.example`
- CHANGELOG(v0.5.6 修复): `deployment/docker/CHANGELOG.md:16-23`
- 项目级规则: `/home/fz/project/OmniDesk/CLAUDE.md`
- 全局 CI/CD 流程: `~/.claude/rules/common/cicd-workflow.md`