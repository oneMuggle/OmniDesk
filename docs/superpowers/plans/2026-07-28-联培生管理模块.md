# 联培生管理模块 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 OmniDesk 添加完整的联培生（联合培养硕博研究生）管理子模块，覆盖月度报告、专家组打分、A 档名额优先算法、补助自动计算与联培生管理员复核。

**Architecture:** 新增单 Django app `joint_students`，1:1 扩展 `personnel.Personnel` 主表（不动原表）。复用 `users.CustomUser` + `permissions.GroupPagePermission` + `notifications.Notification`。Celery Beat 月度自动触发考核批次（可配置 cron 日），联培生管理员可手动提前触发同一任务。后端 DRF + 前端 React 按 4 类角色（管理员/专家/联培生/导师）分页面。

**Tech Stack:**
- Django 4.2 + Django REST Framework
- PostgreSQL (生产) / SQLite-in-memory (测试)
- Celery 5 + Beat（已就绪）
- React 18.3 + Vite 5 + Ant Design 5
- TanStack React Query 5 (前端缓存)
- pytest + pytest-django (后端测试)
- Jest + React Testing Library (前端测试)

---

## Global Constraints

以下约束**隐式适用于每个 task**，实现者无需在每个 task 中重复：

- **Python 版本**：3.10（与项目统一；详见 CLAUDE.md）
- **测试数据库**：使用 `omni_desk_backend.settings.test` 配置（in-memory SQLite + MD5 password hasher）
- **测试命令**：`pytest --ds=omni_desk_backend.settings.test omni_desk_backend/joint_students/`
- **迁移命名**：使用 `python manage.py makemigrations joint_students` 自动生成，data migration 手动编写
- **语言**：所有代码注释、commit message、PR 描述用中文（CLAUDE.md 全局要求）
- **Personnel 主表零修改**：本模块不修改 `personnel/models.py`、不修改现有 `personnel/migrations/`
- **权限类路径**：`omni_desk_backend/users/permissions.py` 已有 `IsAdminOrReadOnly`、`IsAdminOrManagerOrReadOnly`；本模块新增自定义权限类放在 `omni_desk_backend/joint_students/permissions.py`
- **货币计算**：用 `Decimal` 类型，不用 `float`
- **API 响应**：复用 DRF 默认 JSON 响应格式（无自定义 envelope）
- **前端 URL 前缀**：所有 API 调用通过 `/api/joint-students/*`，由 Vite 代理到后端
- **浏览器兼容**：必须支持 Windows 7 + Chrome 109（避免 ES2022+ 语法）
- **离线优先**：所有依赖本地打包，禁止 CDN/外部资源引用
- **测试覆盖率目标**：模型/服务/API 层 ≥ 80%

---

## File Structure

实施时按以下结构创建/修改文件。详细每个文件的职责已在设计文档 `docs/superpowers/specs/2026-07-28-联培生管理模块-design.md` "涉及的文件与模块" 章节。

**新增（后端 19 个）**：
```
omni_desk_backend/joint_students/
├── __init__.py
├── apps.py
├── models.py                   (5 张模型)
├── serializers.py
├── views.py
├── urls.py
├── permissions.py
├── admin.py
├── tasks.py
├── services/{__init__.py, grading.py, stipend.py, cycle.py}
├── management/commands/create_assessment_cycle.py
└── migrations/{0001_initial.py, 0002_seed_groups.py, 0003_seed_notification_types.py, 0004_seed_page_routes.py}
```

**新增（测试 8 个）**：
```
omni_desk_backend/joint_students/tests/
├── test_models.py
├── test_grading.py
├── test_stipend.py
├── test_cycle.py
├── test_tasks.py
├── test_api.py
├── conftest.py
└── factories.py
```

**新增（前端 18 个）**：
```
omni_desk_frontend/src/features/joint-students/
├── api/{client.js, students.js, reports.js, cycles.js, scores.js, stipends.js}
├── pages/admin/{StudentListPage.jsx, StudentEditPage.jsx, StudentDetailPage.jsx, ReportReviewPage.jsx, CycleManagementPage.jsx, CycleDetailPage.jsx, StipendReviewPage.jsx}
├── pages/expert/ExpertScoringPage.jsx
├── pages/student/{MyReportsPage.jsx, ReportSubmitPage.jsx, MyStipendsPage.jsx}
├── pages/mentor/MentorOverviewPage.jsx
├── components/{GradeBadge.jsx, StipendBreakdown.jsx, AttendanceInput.jsx, ExpertScoreTable.jsx}
└── __tests__/{GradeBadge.test.jsx, StipendBreakdown.test.jsx, api/students.test.js}
```

**修改（4 个）**：
- `omni_desk_backend/omni_desk_backend/settings/base.py`（INSTALLED_APPS + JOINT_STUDENT_CYCLE_DAY）
- `omni_desk_backend/omni_desk_backend/celery.py`（Beat schedule）
- `omni_desk_backend/omni_desk_backend/urls.py`（include /api/joint-students/）
- `omni_desk_frontend/src/routes/index.jsx`（12 个新路由）

**文档**：
- `docs/technical/37-joint-students-module.md`（技术章节，实施后创建）
- `docs/user-manual/07-joint-students.md`（用户手册，实施后创建）

---

## Task 1: Django app 骨架与 INSTALLED_APPS 注册

**Files:**
- Create: `omni_desk_backend/joint_students/__init__.py`
- Create: `omni_desk_backend/joint_students/apps.py`
- Modify: `omni_desk_backend/omni_desk_backend/settings/base.py:60-100` (INSTALLED_APPS)

- [ ] **Step 1: 创建 app 目录结构**

```bash
mkdir -p omni_desk_backend/joint_students/{services,management/commands,migrations,tests}
touch omni_desk_backend/joint_students/__init__.py
touch omni_desk_backend/joint_students/services/__init__.py
touch omni_desk_backend/joint_students/management/__init__.py
touch omni_desk_backend/joint_students/management/commands/__init__.py
touch omni_desk_backend/joint_students/migrations/__init__.py
touch omni_desk_backend/joint_students/tests/__init__.py
```

- [ ] **Step 2: 创建 `apps.py`**

文件 `omni_desk_backend/joint_students/apps.py`：

```python
from django.apps import AppConfig


class JointStudentsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "joint_students"
    verbose_name = "联培生管理"
```

- [ ] **Step 3: 在 `settings/base.py` 注册 app**

修改 `omni_desk_backend/omni_desk_backend/settings/base.py`：
- 在 `INSTALLED_APPS` 列表末尾追加：`"joint_students.apps.JointStudentsConfig",`
- 在文件末尾添加配置：

```python
# 联培生考核批次自动触发日（每月 N 号的 02:00 运行 Celery 任务）
JOINT_STUDENT_CYCLE_DAY = int(os.getenv("JOINT_STUDENT_CYCLE_DAY", "1"))
```

- [ ] **Step 4: 验证 Django check 通过**

```bash
cd omni_desk_backend && python manage.py check
```

预期：`System check identified no issues (0 silenced).`

- [ ] **Step 5: 提交**

```bash
git add omni_desk_backend/joint_students/ omni_desk_backend/omni_desk_backend/settings/base.py
git commit -m "feat(joint-students): 创建 app 骨架并注册到 INSTALLED_APPS"
```

---

## Task 2: 实现 5 个数据模型

**Files:**
- Create: `omni_desk_backend/joint_students/tests/factories.py`
- Create: `omni_desk_backend/joint_students/tests/test_models.py`
- Create: `omni_desk_backend/joint_students/models.py`
- Generated: `omni_desk_backend/joint_students/migrations/0001_initial.py`

- [ ] **Step 1: 写测试 fixtures**

文件 `omni_desk_backend/joint_students/tests/factories.py`：

```python
"""测试用工厂函数。"""
from datetime import date
from decimal import Decimal

from personnel.models import Personnel
from users.models import CustomUser

from joint_students.models import (
    AssessmentCycle,
    ExpertScore,
    JointStudent,
    MonthlyReport,
    StipendRecord,
)


def create_personnel(name="测试人员", **kwargs) -> Personnel:
    defaults = {
        "name": name,
        "hire_date": date(2024, 1, 1),
        "department": "测试部",
        "status": "active",
    }
    defaults.update(kwargs)
    return Personnel.objects.create(**defaults)


def create_user(username="testuser", is_superuser=False, **kwargs) -> CustomUser:
    defaults = {
        "username": username,
        "is_superuser": is_superuser,
        "is_staff": is_superuser,
        "email": f"{username}@example.com",
    }
    defaults.update(kwargs)
    return CustomUser.objects.create(**defaults)


def create_joint_student(personnel=None, student_type="master", mentor=None, **kwargs) -> JointStudent:
    if personnel is None:
        personnel = create_personnel()
    defaults = {
        "personnel": personnel,
        "student_type": student_type,
        "student_id": f"S{personnel.id:08d}",
        "enrollment_date": date(2024, 9, 1),
        "mentor": mentor,
        "is_active": True,
    }
    defaults.update(kwargs)
    return JointStudent.objects.create(**defaults)


def create_cycle(year=2026, month=7, **kwargs) -> AssessmentCycle:
    defaults = {
        "year": year,
        "month": month,
        "cycle_start_date": date(year, month, 1),
        "cycle_end_date": date(year, month, 25),
        "scoring_deadline": date(year, month, 28),
        "status": "collecting",
        "trigger_source": "manual",
    }
    defaults.update(kwargs)
    return AssessmentCycle.objects.create(**defaults)


def create_report(joint_student=None, year=2026, month=7, status="draft", **kwargs) -> MonthlyReport:
    if joint_student is None:
        joint_student = create_joint_student()
    defaults = {
        "joint_student": joint_student,
        "year": year,
        "month": month,
        "work_progress": "本月完成了 X 工作",
        "work_highlights": "亮点 Y",
        "attendance_days_actual": Decimal("22.0"),
        "attendance_days_expected": Decimal("22.0"),
        "status": status,
    }
    defaults.update(kwargs)
    return MonthlyReport.objects.create(**defaults)


def create_score(cycle=None, expert=None, joint_student=None, score=Decimal("85.0"), **kwargs) -> ExpertScore:
    defaults = {
        "cycle": cycle,
        "expert": expert,
        "joint_student": joint_student,
        "score": score,
        "submitted_at": date(2026, 7, 26),
        "is_locked": True,
    }
    defaults.update(kwargs)
    return ExpertScore.objects.create(**defaults)


def create_stipend(cycle=None, joint_student=None, grade="B", **kwargs) -> StipendRecord:
    defaults = {
        "cycle": cycle,
        "joint_student": joint_student,
        "average_score": Decimal("85.0"),
        "rank_in_cycle": 1,
        "grade": grade,
        "base_amount": Decimal("3000.00"),
        "grade_coefficient": Decimal("0.80"),
        "attendance_ratio": Decimal("1.00"),
        "final_amount": Decimal("2400.00"),
        "status": "pending",
    }
    defaults.update(kwargs)
    return StipendRecord.objects.create(**defaults)
```

- [ ] **Step 2: 写模型测试**

文件 `omni_desk_backend/joint_students/tests/test_models.py`：

```python
"""模型层测试: 验证字段约束。"""
import pytest
from django.db import IntegrityError

from joint_students.tests.factories import (
    create_cycle,
    create_joint_student,
    create_personnel,
    create_report,
    create_score,
    create_stipend,
    create_user,
)


@pytest.mark.django_db
class TestJointStudent:
    def test_unique_personnel_constraint(self):
        """OneToOne 约束: 同一 personnel 不能创建两次。"""
        p = create_personnel()
        from joint_students.models import JointStudent
        JointStudent.objects.create(
            personnel=p, student_type="master", student_id="S001",
            enrollment_date="2024-09-01",
        )
        with pytest.raises(IntegrityError):
            JointStudent.objects.create(
                personnel=p, student_type="phd", student_id="S002",
                enrollment_date="2024-09-01",
            )

    def test_student_id_unique(self):
        """student_id 全局唯一。"""
        p1 = create_personnel(name="张三")
        p2 = create_personnel(name="李四")
        from joint_students.models import JointStudent
        JointStudent.objects.create(
            personnel=p1, student_type="master", student_id="SAME001",
            enrollment_date="2024-09-01",
        )
        with pytest.raises(IntegrityError):
            JointStudent.objects.create(
                personnel=p2, student_type="master", student_id="SAME001",
                enrollment_date="2024-09-01",
            )


@pytest.mark.django_db
class TestMonthlyReport:
    def test_unique_per_month(self):
        """同一联培生同月只能一份报告。"""
        js = create_joint_student()
        create_report(joint_student=js, year=2026, month=7)
        with pytest.raises(IntegrityError):
            create_report(joint_student=js, year=2026, month=7)

    def test_different_months_allowed(self):
        """不同月份的报告可以共存。"""
        js = create_joint_student()
        r1 = create_report(joint_student=js, year=2026, month=7)
        r2 = create_report(joint_student=js, year=2026, month=8)
        assert r1.id != r2.id


@pytest.mark.django_db
class TestAssessmentCycle:
    def test_unique_per_year_month(self):
        create_cycle(year=2026, month=7)
        with pytest.raises(IntegrityError):
            create_cycle(year=2026, month=7)

    def test_different_months_allowed(self):
        c1 = create_cycle(year=2026, month=7)
        c2 = create_cycle(year=2026, month=8)
        assert c1.id != c2.id


@pytest.mark.django_db
class TestExpertScore:
    def test_unique_per_cycle_expert_student(self):
        cycle = create_cycle()
        expert = create_user(username="expert1")
        js = create_joint_student()
        create_score(cycle=cycle, expert=expert, joint_student=js)
        with pytest.raises(IntegrityError):
            create_score(cycle=cycle, expert=expert, joint_student=js)


@pytest.mark.django_db
class TestStipendRecord:
    def test_unique_per_cycle_student(self):
        cycle = create_cycle()
        js = create_joint_student()
        create_stipend(cycle=cycle, joint_student=js)
        with pytest.raises(IntegrityError):
            create_stipend(cycle=cycle, joint_student=js)
```

- [ ] **Step 3: 运行测试, 确认 ImportError**

```bash
cd omni_desk_backend && pytest --ds=omni_desk_backend.settings.test omni_desk_backend/joint_students/tests/test_models.py -v
```

预期：`ModuleNotFoundError: No module named 'joint_students.models'`。

- [ ] **Step 4: 实现 `models.py`**

文件 `omni_desk_backend/joint_students/models.py`：

```python
"""联培生管理 - 数据模型 (5 张表)。"""
from django.conf import settings
from django.core.validators import MaxValueValidator, MinValueValidator
from django.db import models
from personnel.models import Personnel


class JointStudent(models.Model):
    """联培生扩展表 (1:1 关联 Personnel)。"""

    STUDENT_TYPE_MASTER = "master"
    STUDENT_TYPE_PHD = "phd"
    STUDENT_TYPE_CHOICES = [
        (STUDENT_TYPE_MASTER, "硕士"),
        (STUDENT_TYPE_PHD, "博士"),
    ]

    personnel = models.OneToOneField(
        Personnel, on_delete=models.PROTECT, related_name="joint_student",
        verbose_name="关联人员",
    )
    student_type = models.CharField(
        max_length=10, choices=STUDENT_TYPE_CHOICES, verbose_name="培养类型",
    )
    student_id = models.CharField(max_length=50, unique=True, verbose_name="学号")
    enrollment_date = models.DateField(verbose_name="入学日期")
    graduation_date = models.DateField(null=True, blank=True, verbose_name="毕业日期")
    mentor = models.ForeignKey(
        Personnel, on_delete=models.SET_NULL, null=True, blank=True,
        related_name="mentored_joint_students", verbose_name="导师",
    )
    is_active = models.BooleanField(default=True, verbose_name="在读")
    created_at = models.DateTimeField(auto_now_add=True, verbose_name="创建时间")
    updated_at = models.DateTimeField(auto_now=True, verbose_name="更新时间")

    class Meta:
        verbose_name = "联培生"
        verbose_name_plural = "联培生管理"
        indexes = [models.Index(fields=["student_type", "is_active"], name="js_type_active_idx")]

    def __str__(self):
        return f"{self.student_id} - {self.personnel.name}"


class MonthlyReport(models.Model):
    """月度报告。一人一月一份。"""

    STATUS_DRAFT = "draft"
    STATUS_SUBMITTED = "submitted"
    STATUS_APPROVED = "approved"
    STATUS_REJECTED = "rejected"
    STATUS_CHOICES = [
        (STATUS_DRAFT, "草稿"),
        (STATUS_SUBMITTED, "已提交"),
        (STATUS_APPROVED, "已通过"),
        (STATUS_REJECTED, "已驳回"),
    ]

    joint_student = models.ForeignKey(
        JointStudent, on_delete=models.CASCADE,
        related_name="monthly_reports", verbose_name="联培生",
    )
    year = models.PositiveSmallIntegerField(verbose_name="年")
    month = models.PositiveSmallIntegerField(verbose_name="月")
    work_progress = models.TextField(verbose_name="工作进展")
    work_highlights = models.TextField(verbose_name="工作亮点")
    attendance_days_actual = models.DecimalField(
        max_digits=5, decimal_places=1, verbose_name="实际出勤天数",
    )
    attendance_days_expected = models.DecimalField(
        max_digits=5, decimal_places=1, default=22, verbose_name="应出勤天数",
    )
    attendance_notes = models.TextField(blank=True, verbose_name="出勤说明")
    status = models.CharField(
        max_length=10, choices=STATUS_CHOICES, default=STATUS_DRAFT, verbose_name="状态",
    )
    submitted_at = models.DateTimeField(null=True, blank=True, verbose_name="提交时间")
    reviewed_at = models.DateTimeField(null=True, blank=True, verbose_name="审核时间")
    reviewer = models.ForeignKey(
        settings.AUTH_USER_MODEL, on_delete=models.SET_NULL,
        null=True, blank=True, related_name="reviewed_reports", verbose_name="审核人",
    )
    reviewer_comment = models.TextField(blank=True, verbose_name="审核意见")
    created_at = models.DateTimeField(auto_now_add=True, verbose_name="创建时间")
    updated_at = models.DateTimeField(auto_now=True, verbose_name="更新时间")

    class Meta:
        verbose_name = "月度报告"
        verbose_name_plural = "月度报告"
        ordering = ["-year", "-month"]
        constraints = [
            models.UniqueConstraint(
                fields=["joint_student", "year", "month"],
                name="report_unique_per_student_month",
            ),
        ]

    def __str__(self):
        return f"{self.joint_student} {self.year}-{self.month:02d}"


class AssessmentCycle(models.Model):
    """考核批次。一月一批。"""

    STATUS_COLLECTING = "collecting"
    STATUS_CLOSED = "closed"
    STATUS_FINALIZED = "finalized"
    STATUS_CHOICES = [
        (STATUS_COLLECTING, "收集评分中"),
        (STATUS_CLOSED, "已截止"),
        (STATUS_FINALIZED, "已归档"),
    ]

    TRIGGER_AUTO = "auto"
    TRIGGER_MANUAL = "manual"
    TRIGGER_CHOICES = [(TRIGGER_AUTO, "Celery自动"), (TRIGGER_MANUAL, "管理员手动")]

    year = models.PositiveSmallIntegerField(verbose_name="年")
    month = models.PositiveSmallIntegerField(verbose_name="月")
    cycle_start_date = models.DateField(verbose_name="报告提交窗口开始")
    cycle_end_date = models.DateField(verbose_name="报告审核截止")
    scoring_deadline = models.DateField(verbose_name="专家打分截止")
    status = models.CharField(
        max_length=15, choices=STATUS_CHOICES, default=STATUS_COLLECTING, verbose_name="状态",
    )
    trigger_source = models.CharField(
        max_length=10, choices=TRIGGER_CHOICES, default=TRIGGER_MANUAL, verbose_name="触发来源",
    )
    created_at = models.DateTimeField(auto_now_add=True, verbose_name="创建时间")
    created_by = models.ForeignKey(
        settings.AUTH_USER_MODEL, on_delete=models.SET_NULL,
        null=True, blank=True, related_name="created_cycles", verbose_name="创建人",
    )

    class Meta:
        verbose_name = "考核批次"
        verbose_name_plural = "考核批次"
        ordering = ["-year", "-month"]
        constraints = [
            models.UniqueConstraint(fields=["year", "month"], name="cycle_unique_per_month"),
        ]

    def __str__(self):
        return f"{self.year}-{self.month:02d}"


class ExpertScore(models.Model):
    """专家打分。"""

    cycle = models.ForeignKey(
        AssessmentCycle, on_delete=models.CASCADE,
        related_name="scores", verbose_name="考核批次",
    )
    expert = models.ForeignKey(
        settings.AUTH_USER_MODEL, on_delete=models.PROTECT,
        related_name="expert_scores", verbose_name="专家",
    )
    joint_student = models.ForeignKey(
        JointStudent, on_delete=models.PROTECT,
        related_name="expert_scores", verbose_name="联培生",
    )
    score = models.DecimalField(
        max_digits=5, decimal_places=2,
        validators=[MinValueValidator(0), MaxValueValidator(100)],
        verbose_name="分数",
    )
    comment = models.TextField(blank=True, verbose_name="评语")
    submitted_at = models.DateTimeField(verbose_name="提交时间")
    is_locked = models.BooleanField(default=True, verbose_name="是否锁定")

    class Meta:
        verbose_name = "专家打分"
        verbose_name_plural = "专家打分"
        constraints = [
            models.UniqueConstraint(
                fields=["cycle", "expert", "joint_student"],
                name="score_unique_per_expert_cycle_student",
            ),
        ]

    def __str__(self):
        return f"{self.expert} → {self.joint_student}: {self.score}"


class StipendRecord(models.Model):
    """补助记录。"""

    STATUS_PENDING = "pending"
    STATUS_LOCKED = "locked"
    STATUS_CHOICES = [(STATUS_PENDING, "待复核"), (STATUS_LOCKED, "已锁定")]

    GRADE_A = "A"
    GRADE_B = "B"
    GRADE_CHOICES = [(GRADE_A, "A档"), (GRADE_B, "B档")]

    cycle = models.ForeignKey(
        AssessmentCycle, on_delete=models.CASCADE,
        related_name="stipends", verbose_name="考核批次",
    )
    joint_student = models.ForeignKey(
        JointStudent, on_delete=models.PROTECT,
        related_name="stipends", verbose_name="联培生",
    )
    average_score = models.DecimalField(max_digits=5, decimal_places=2, verbose_name="平均分")
    rank_in_cycle = models.PositiveSmallIntegerField(verbose_name="周期内排名")
    grade = models.CharField(max_length=1, choices=GRADE_CHOICES, verbose_name="档次")
    base_amount = models.DecimalField(max_digits=10, decimal_places=2, verbose_name="基本额度")
    grade_coefficient = models.DecimalField(max_digits=4, decimal_places=2, verbose_name="档次系数")
    attendance_ratio = models.DecimalField(max_digits=5, decimal_places=2, verbose_name="出勤比")
    final_amount = models.DecimalField(max_digits=10, decimal_places=2, verbose_name="最终金额")
    status = models.CharField(
        max_length=10, choices=STATUS_CHOICES, default=STATUS_PENDING, verbose_name="状态",
    )
    locked_at = models.DateTimeField(null=True, blank=True, verbose_name="锁定时间")
    locked_by = models.ForeignKey(
        settings.AUTH_USER_MODEL, on_delete=models.SET_NULL,
        null=True, blank=True, related_name="locked_stipends", verbose_name="锁定操作人",
    )
    notes = models.TextField(blank=True, verbose_name="备注")
    created_at = models.DateTimeField(auto_now_add=True, verbose_name="创建时间")
    updated_at = models.DateTimeField(auto_now=True, verbose_name="更新时间")

    class Meta:
        verbose_name = "补助记录"
        verbose_name_plural = "补助记录"
        constraints = [
            models.UniqueConstraint(
                fields=["cycle", "joint_student"], name="stipend_unique_per_cycle_student",
            ),
        ]

    def __str__(self):
        return f"{self.joint_student} {self.cycle}: {self.final_amount} ({self.grade})"
```

- [ ] **Step 5: 生成并应用 migration**

```bash
cd omni_desk_backend && python manage.py makemigrations joint_students
cd omni_desk_backend && python manage.py migrate
```

预期：生成 `0001_initial.py` 并应用成功。

- [ ] **Step 6: 跑测试, 确认全部通过**

```bash
cd omni_desk_backend && pytest --ds=omni_desk_backend.settings.test omni_desk_backend/joint_students/tests/test_models.py -v
```

预期：所有测试 PASS（7 个 test methods）。

- [ ] **Step 7: 提交**

```bash
git add omni_desk_backend/joint_students/
git commit -m "feat(joint-students): 实现 5 个数据模型与模型测试"
```

---

## Task 3: data migration - 创建 2 个 Group + 12 个 PageRoute

**Files:**
- Create: `omni_desk_backend/joint_students/migrations/0002_seed_groups.py`
- Create: `omni_desk_backend/joint_students/migrations/0003_seed_page_routes.py`

**Interfaces (后续 task 依赖):**
- `Group.objects.get(name="联培生管理员")` 必须存在
- `Group.objects.get(name="考核专家组")` 必须存在
- `PageRoute.objects.filter(path__startswith="/joint-students/")` 必须返回 12 行

- [ ] **Step 1: 创建 `0002_seed_groups.py`**

文件 `omni_desk_backend/joint_students/migrations/0002_seed_groups.py`：

```python
"""创建联培生模块的 2 个固定 Group (空 Group, 成员由 admin 手动添加)。"""
from django.db import migrations


def create_groups(apps, schema_editor):
    Group = apps.get_model("auth", "Group")
    Group.objects.get_or_create(name="联培生管理员")
    Group.objects.get_or_create(name="考核专家组")


def remove_groups(apps, schema_editor):
    Group = apps.get_model("auth", "Group")
    Group.objects.filter(name__in=["联培生管理员", "考核专家组"]).delete()


class Migration(migrations.Migration):
    dependencies = [("joint_students", "0001_initial")]
    operations = [migrations.RunPython(create_groups, remove_groups)]
```

- [ ] **Step 2: 创建 `0003_seed_page_routes.py`**

文件 `omni_desk_backend/joint_students/migrations/0003_seed_page_routes.py`：

```python
"""创建联培生模块的 12 个 PageRoute (用于前端 ProtectedRoute 权限控制)。"""
from django.db import migrations

PAGE_ROUTES_DATA = [
    ("联培生-管理员首页", "/joint-students/admin/students", "StudentListPage", None),
    ("联培生-创建联培生", "/joint-students/admin/students/new", "StudentEditPage", "联培生-管理员首页"),
    ("联培生-联培生详情", "/joint-students/admin/students/:id", "StudentDetailPage", "联培生-管理员首页"),
    ("联培生-报告审核", "/joint-students/admin/reports", "ReportReviewPage", None),
    ("联培生-批次管理", "/joint-students/admin/cycles", "CycleManagementPage", None),
    ("联培生-批次详情", "/joint-students/admin/cycles/:id", "CycleDetailPage", "联培生-批次管理"),
    ("联培生-补助复核", "/joint-students/admin/stipends", "StipendReviewPage", None),
    ("联培生-专家打分", "/joint-students/expert/scoring", "ExpertScoringPage", None),
    ("联培生-我的报告", "/joint-students/student/reports", "MyReportsPage", None),
    ("联培生-填报报告", "/joint-students/student/reports/new", "ReportSubmitPage", "联培生-我的报告"),
    ("联培生-我的补助", "/joint-students/student/stipends", "MyStipendsPage", None),
    ("联培生-导师视图", "/joint-students/mentor/overview", "MentorOverviewPage", None),
]


def create_page_routes(apps, schema_editor):
    PageRoute = apps.get_model("permissions", "PageRoute")
    name_to_route = {}
    for name, path, component, parent_name in PAGE_ROUTES_DATA:
        parent = name_to_route.get(parent_name) if parent_name else None
        route, _ = PageRoute.objects.get_or_create(
            path=path,
            defaults={"name": name, "component": component, "parent": parent},
        )
        name_to_route[name] = route


def remove_page_routes(apps, schema_editor):
    PageRoute = apps.get_model("permissions", "PageRoute")
    paths = [r[1] for r in PAGE_ROUTES_DATA]
    PageRoute.objects.filter(path__in=paths).delete()


class Migration(migrations.Migration):
    dependencies = [
        ("joint_students", "0002_seed_groups"),
        ("permissions", "0001_initial"),
    ]
    operations = [migrations.RunPython(create_page_routes, remove_page_routes)]
```

- [ ] **Step 3: 应用所有 migration**

```bash
cd omni_desk_backend && python manage.py migrate joint_students
```

预期：3 个 migration 全部应用成功（0001 自动生成 + 0002 + 0003）。

- [ ] **Step 4: 验证数据已写入**

```bash
cd omni_desk_backend && python manage.py shell -c "
from django.contrib.auth.models import Group
from permissions.models import PageRoute
print('Groups:', list(Group.objects.filter(name__in=['联培生管理员', '考核专家组']).values_list('name', flat=True)))
print('PageRoutes count:', PageRoute.objects.filter(path__startswith='/joint-students/').count())
"
```

预期：
```
Groups: ['联培生管理员', '考核专家组']
PageRoutes count: 12
```

- [ ] **Step 5: 提交**

```bash
git add omni_desk_backend/joint_students/migrations/
git commit -m "feat(joint-students): data migration 创建 Group/PageRoute"
```

---

## Task 4: services/grading.py - A 档名额优先算法

**Files:**
- Create: `omni_desk_backend/joint_students/services/grading.py`
- Create: `omni_desk_backend/joint_students/tests/test_grading.py`

**Interfaces (后续 task 依赖):**
- `assign_grades(cycle: AssessmentCycle) -> list[dict]`
- 返回列表每项: `{'js_id': int, 'rank': int, 'grade': 'A'|'B', 'avg_score': Decimal}`
- 边界：人数 < 3 全 B；同分按 ID 升序稳定排序；无打分则跳过

- [ ] **Step 1: 写 grading 测试**

文件 `omni_desk_backend/joint_students/tests/test_grading.py`：

```python
"""A 档名额优先算法测试。"""
from datetime import date
from decimal import Decimal

import pytest

from joint_students.services.grading import assign_grades
from joint_students.tests.factories import (
    create_cycle,
    create_joint_student,
    create_report,
    create_score,
    create_user,
)


def _setup_cycle_with_students(n, scores):
    """创建 cycle + n 个联培生 + 每个联培生指定分数的 expert score。
    scores: list of decimal, 与 n 个联培生一一对应。
    """
    cycle = create_cycle()
    js_list = [create_joint_student() for _ in range(n)]
    for js, s in zip(js_list, scores):
        create_report(joint_student=js, year=2026, month=7, status="approved")
        create_score(cycle=cycle, joint_student=js, expert=create_user(), score=s)
    return cycle, js_list


@pytest.mark.django_db
class TestAssignGrades:
    def test_assign_grades_basic_10_students(self):
        """10 人 → 4 A 6 B (40% 向下取整)。"""
        scores = [Decimal(str(x)) for x in [98, 95, 92, 88, 86, 82, 80, 78, 75, 70]]
        cycle, js_list = _setup_cycle_with_students(10, scores)
        result = assign_grades(cycle)
        grades = {r["js_id"]: r["grade"] for r in result}
        # 前 4 名 → A 档
        for i in range(4):
            assert grades[js_list[i].id] == "A", f"rank {i+1} 应为 A"
        # 后 6 名 → B 档
        for i in range(4, 10):
            assert grades[js_list[i].id] == "B", f"rank {i+1} 应为 B"

    def test_assign_grades_all_above_90_still_capped_at_40(self):
        """即使所有分数 ≥90, 名额硬限制仍生效。"""
        scores = [Decimal("95")] * 10
        cycle, js_list = _setup_cycle_with_students(10, scores)
        result = assign_grades(cycle)
        a_count = sum(1 for r in result if r["grade"] == "A")
        b_count = sum(1 for r in result if r["grade"] == "B")
        assert a_count == 4
        assert b_count == 6

    def test_assign_grades_below_3_all_b(self):
        """< 3 人全部 B 档 (避免单人硬升 A)。"""
        scores = [Decimal("95"), Decimal("90")]
        cycle, js_list = _setup_cycle_with_students(2, scores)
        result = assign_grades(cycle)
        assert all(r["grade"] == "B" for r in result)

    def test_assign_grades_exactly_3_one_a(self):
        """3 人 → 1 A 2 B。"""
        scores = [Decimal("95"), Decimal("88"), Decimal("80")]
        cycle, js_list = _setup_cycle_with_students(3, scores)
        result = assign_grades(cycle)
        a_count = sum(1 for r in result if r["grade"] == "A")
        assert a_count == 1
        # 最高分应为 A 档
        a_record = next(r for r in result if r["grade"] == "A")
        assert a_record["avg_score"] == Decimal("95")

    def test_assign_grades_tie_breaking_by_id(self):
        """同分时按 joint_student.id 升序稳定排序。"""
        scores = [Decimal("90"), Decimal("90"), Decimal("90"), Decimal("80")]
        cycle, js_list = _setup_cycle_with_students(4, scores)
        result = assign_grades(cycle)
        a_records = [r for r in result if r["grade"] == "A"]
        # 4 人 → 1 A 档 (floor(4*0.4)=1)，最低 ID 的同分者应优先 A 档
        assert len(a_records) == 1
        expected_a_id = min(js_list[i].id for i in range(3))  # 前 3 个同分中 ID 最小的
        assert a_records[0]["js_id"] == expected_a_id

    def test_assign_grades_no_scores_skipped(self):
        """无专家打分的联培生不参与 A/B 档分配。"""
        cycle = create_cycle()
        js_with_score = create_joint_student()
        js_without_score = create_joint_student()
        create_report(joint_student=js_with_score, status="approved")
        create_report(joint_student=js_without_score, status="approved")
        create_score(cycle=cycle, joint_student=js_with_score, expert=create_user(), score=Decimal("90"))
        result = assign_grades(cycle)
        assert len(result) == 1
        assert result[0]["js_id"] == js_with_score.id

    def test_assign_grades_returns_rank(self):
        """返回的 rank 从 1 开始递增。"""
        scores = [Decimal("95"), Decimal("88"), Decimal("80")]
        cycle, _ = _setup_cycle_with_students(3, scores)
        result = assign_grades(cycle)
        ranks = sorted(r["rank"] for r in result)
        assert ranks == [1, 2, 3]
```

- [ ] **Step 2: 跑测试, 确认 ImportError**

```bash
cd omni_desk_backend && pytest --ds=omni_desk_backend.settings.test omni_desk_backend/joint_students/tests/test_grading.py -v
```

预期：`ModuleNotFoundError: No module named 'joint_students.services.grading'`。

- [ ] **Step 3: 实现 `services/grading.py`**

文件 `omni_desk_backend/joint_students/services/grading.py`：

```python
"""A 档名额优先算法 (名额硬限制 ≤ 40%)。"""
import logging
import math
from decimal import Decimal

from django.db.models import Avg

from joint_students.models import AssessmentCycle, ExpertScore, MonthlyReport

logger = logging.getLogger(__name__)


def assign_grades(cycle: AssessmentCycle) -> list[dict]:
    """根据专家打分的均分给本月联培生分 A/B 档。

    Args:
        cycle: 本月考核批次。

    Returns:
        列表, 每项: {js_id, rank, grade, avg_score}。
        - grade: 'A' (前 40%) 或 'B' (其余)
        - 联培生 < 3 人 → 全部 B
        - 无打分的联培生跳过
        - 同分时按 joint_student.id 升序稳定排序
    """
    # 1. 找本月所有已审核通过的月度报告对应的联培生
    approved_student_ids = (
        MonthlyReport.objects
        .filter(year=cycle.year, month=cycle.month, status=MonthlyReport.STATUS_APPROVED)
        .values_list("joint_student_id", flat=True)
        .distinct()
    )

    # 2. 计算每位联培生的专家均分
    student_scores: list[tuple[int, Decimal]] = []
    for js_id in approved_student_ids:
        avg = ExpertScore.objects.filter(
            cycle=cycle, joint_student_id=js_id,
        ).aggregate(avg=Avg("score"))["avg"]
        if avg is None:
            continue
        student_scores.append((js_id, Decimal(avg).quantize(Decimal("0.01"))))

    if not student_scores:
        return []

    # 3. 按分数降序排序 (同分按 ID 升序稳定排序)
    student_scores.sort(key=lambda x: (-x[1], x[0]))

    # 4. 前 40% 为 A 档 (向下取整, < 3 全 B)
    total = len(student_scores)
    a_count = math.floor(total * 0.4)

    records = []
    for rank, (js_id, avg_score) in enumerate(student_scores, start=1):
        grade = "A" if rank <= a_count else "B"
        records.append({
            "js_id": js_id,
            "rank": rank,
            "grade": grade,
            "avg_score": avg_score,
        })

    if a_count == 0 and total > 0:
        logger.info(f"周期 {cycle.year}-{cycle.month:02d} 总人数 {total} < 3, 全部 B 档")

    return records
```

- [ ] **Step 4: 跑测试, 确认全部通过**

```bash
cd omni_desk_backend && pytest --ds=omni_desk_backend.settings.test omni_desk_backend/joint_students/tests/test_grading.py -v
```

预期：所有 7 个 test methods PASS。

- [ ] **Step 5: 提交**

```bash
git add omni_desk_backend/joint_students/services/grading.py omni_desk_backend/joint_students/tests/test_grading.py
git commit -m "feat(joint-students): 实现 A 档名额优先算法与边界 case 测试"
```

---

## Task 5: services/stipend.py - 补助计算

**Files:**
- Create: `omni_desk_backend/joint_students/services/stipend.py`
- Create: `omni_desk_backend/joint_students/tests/test_stipend.py`

**Interfaces:**
- `compute_stipend_amount(js, grade, attendance_actual, attendance_expected) -> Decimal`
- `compute_attendance_ratio(actual, expected) -> Decimal` (上限 1.0, 应出勤 0 防御)

- [ ] **Step 1: 写 stipend 测试**

文件 `omni_desk_backend/joint_students/tests/test_stipend.py`：

```python
"""补助计算公式测试。"""
from decimal import Decimal

import pytest

from joint_students.services.stipend import compute_attendance_ratio, compute_stipend_amount
from joint_students.tests.factories import create_joint_student


@pytest.mark.django_db
class TestComputeStipendAmount:
    def test_master_a_full_attendance(self):
        """硕士 A 档 全勤: 3000 × 1.00 × 1.0 = 3000.00"""
        js = create_joint_student(student_type="master")
        result = compute_stipend_amount(js, "A", Decimal("22"), Decimal("22"))
        assert result == Decimal("3000.00")

    def test_master_b_full_attendance(self):
        """硕士 B 档 全勤: 3000 × 0.80 × 1.0 = 2400.00"""
        js = create_joint_student(student_type="master")
        result = compute_stipend_amount(js, "B", Decimal("22"), Decimal("22"))
        assert result == Decimal("2400.00")

    def test_phd_a_full_attendance(self):
        """博士 A 档 全勤: 6000 × 1.00 × 1.0 = 6000.00"""
        js = create_joint_student(student_type="phd")
        result = compute_stipend_amount(js, "A", Decimal("22"), Decimal("22"))
        assert result == Decimal("6000.00")

    def test_phd_b_partial_attendance(self):
        """博士 B 档 出勤 11/22 (50%): 6000 × 0.80 × 0.5 = 2400.00"""
        js = create_joint_student(student_type="phd")
        result = compute_stipend_amount(js, "B", Decimal("11"), Decimal("22"))
        assert result == Decimal("2400.00")

    def test_master_a_partial_attendance(self):
        """硕士 A 档 出勤 10/22: 3000 × 1.00 × (10/22) = 1363.64"""
        js = create_joint_student(student_type="master")
        result = compute_stipend_amount(js, "A", Decimal("10"), Decimal("22"))
        assert result == Decimal("1363.64")

    def test_attendance_over_expected_capped_at_1(self):
        """出勤 > 应出勤 → ratio 限制为 1.0"""
        js = create_joint_student(student_type="master")
        result = compute_stipend_amount(js, "A", Decimal("30"), Decimal("22"))
        assert result == Decimal("3000.00")  # 不会因为超勤变 4090.91

    def test_zero_expected_attendance_defensive(self):
        """应出勤 0 → ratio = 1.0 (防御, 按满勤)"""
        js = create_joint_student(student_type="master")
        result = compute_stipend_amount(js, "A", Decimal("0"), Decimal("0"))
        assert result == Decimal("3000.00")


@pytest.mark.django_db
class TestComputeAttendanceRatio:
    def test_normal_case(self):
        assert compute_attendance_ratio(Decimal("11"), Decimal("22")) == Decimal("0.50")

    def test_capped_at_one(self):
        assert compute_attendance_ratio(Decimal("30"), Decimal("22")) == Decimal("1.00")

    def test_zero_expected_returns_one(self):
        assert compute_attendance_ratio(Decimal("0"), Decimal("0")) == Decimal("1.00")

    def test_negative_expected_returns_one(self):
        assert compute_attendance_ratio(Decimal("0"), Decimal("-1")) == Decimal("1.00")
```

- [ ] **Step 2: 跑测试, 确认 ImportError**

```bash
cd omni_desk_backend && pytest --ds=omni_desk_backend.settings.test omni_desk_backend/joint_students/tests/test_stipend.py -v
```

预期：`ModuleNotFoundError: No module named 'joint_students.services.stipend'`。

- [ ] **Step 3: 实现 `services/stipend.py`**

文件 `omni_desk_backend/joint_students/services/stipend.py`：

```python
"""补助计算公式。

final_amount = base_amount × grade_coefficient × attendance_ratio
"""
import logging
from decimal import Decimal

from joint_students.models import JointStudent

logger = logging.getLogger(__name__)

BASE_AMOUNT_MASTER = Decimal("3000.00")
BASE_AMOUNT_PHD = Decimal("6000.00")

GRADE_COEFFICIENT_A = Decimal("1.00")
GRADE_COEFFICIENT_B = Decimal("0.80")


def compute_attendance_ratio(actual: Decimal, expected: Decimal) -> Decimal:
    """出勤比 = min(actual/expected, 1.0)。
    应出勤 <= 0 时按 1.0 (满勤) 处理。
    """
    if expected <= 0:
        return Decimal("1.00")
    ratio = Decimal(actual) / Decimal(expected)
    return min(ratio, Decimal("1.00")).quantize(Decimal("0.01"))


def compute_stipend_amount(
    js: JointStudent,
    grade: str,
    attendance_actual: Decimal,
    attendance_expected: Decimal,
) -> Decimal:
    """根据联培生类型 + 档次 + 出勤, 计算最终补助金额 (保留 2 位小数)。"""
    base = BASE_AMOUNT_MASTER if js.student_type == JointStudent.STUDENT_TYPE_MASTER else BASE_AMOUNT_PHD
    coef = GRADE_COEFFICIENT_A if grade == JointStudent.GRADE_A else GRADE_COEFFICIENT_B
    ratio = compute_attendance_ratio(attendance_actual, attendance_expected)
    return (base * coef * ratio).quantize(Decimal("0.01"))
```

- [ ] **Step 4: 跑测试, 确认全部通过**

```bash
cd omni_desk_backend && pytest --ds=omni_desk_backend.settings.test omni_desk_backend/joint_students/tests/test_stipend.py -v
```

预期：所有 11 个 test methods PASS。

- [ ] **Step 5: 提交**

```bash
git add omni_desk_backend/joint_students/services/stipend.py omni_desk_backend/joint_students/tests/test_stipend.py
git commit -m "feat(joint-students): 实现补助计算公式 (含出勤上限与应出勤 0 防御)"
```

---

## Task 6: services/cycle.py - 批次生命周期

**Files:**
- Create: `omni_desk_backend/joint_students/services/cycle.py`
- Create: `omni_desk_backend/joint_students/tests/test_cycle.py`

**Interfaces:**
- `create_cycle(year, month, trigger_source='auto', creator=None) -> AssessmentCycle` (幂等)
- `close_cycle(cycle) -> None` (算 grade + 创建 stipend, 通知管理员)

- [ ] **Step 1: 写 cycle 测试**

文件 `omni_desk_backend/joint_students/tests/test_cycle.py`：

```python
"""批次生命周期测试: 创建/截止/算 grade + stipend。"""
from datetime import date
from decimal import Decimal

import pytest

from joint_students.models import AssessmentCycle, StipendRecord
from joint_students.services.cycle import close_cycle, create_cycle
from joint_students.tests.factories import (
    create_cycle,
    create_joint_student,
    create_report,
    create_score,
    create_user,
)


@pytest.mark.django_db
class TestCreateCycle:
    def test_create_new_cycle(self):
        """正常创建新批次。"""
        cycle = create_cycle(year=2026, month=7, status="collecting")
        assert cycle.id is not None
        assert cycle.year == 2026
        assert cycle.month == 7
        assert cycle.status == "collecting"

    def test_create_cycle_idempotent(self):
        """同月二次创建不重复。"""
        c1 = create_cycle(year=2026, month=7)
        c2 = create_cycle(year=2026, month=7)
        assert c1.id == c2.id

    def test_create_cycle_auto_source(self):
        """trigger_source='auto' 用于 Celery 自动触发。"""
        cycle = create_cycle(year=2026, month=7, trigger_source="auto")
        assert cycle.trigger_source == "auto"

    def test_create_cycle_with_creator(self):
        """手动触发时记录 creator。"""
        user = create_user(username="manager1")
        cycle = create_cycle(year=2026, month=7, created_by=user)
        assert cycle.created_by == user


@pytest.mark.django_db
class TestCloseCycle:
    def test_close_creates_stipends_for_all_scored_students(self):
        """close_cycle 为每个有打分的联培生创建 StipendRecord。"""
        cycle = create_cycle()
        js1 = create_joint_student()
        js2 = create_joint_student()
        create_report(joint_student=js1, year=2026, month=7, status="approved")
        create_report(joint_student=js2, year=2026, month=7, status="approved")
        create_score(cycle=cycle, joint_student=js1, expert=create_user(), score=Decimal("95"))
        create_score(cycle=cycle, joint_student=js2, expert=create_user(), score=Decimal("80"))

        close_cycle(cycle)

        cycle.refresh_from_db()
        assert cycle.status == "closed"
        assert StipendRecord.objects.filter(cycle=cycle).count() == 2

    def test_close_assigns_grade_a_to_top_40_percent(self):
        """3 人 → 1 A 2 B (floor(3*0.4)=1)。"""
        cycle = create_cycle()
        js_high = create_joint_student()
        js_mid = create_joint_student()
        js_low = create_joint_student()
        for js in [js_high, js_mid, js_low]:
            create_report(joint_student=js, status="approved")
        create_score(cycle=cycle, joint_student=js_high, expert=create_user(), score=Decimal("95"))
        create_score(cycle=cycle, joint_student=js_mid, expert=create_user(), score=Decimal("85"))
        create_score(cycle=cycle, joint_student=js_low, expert=create_user(), score=Decimal("70"))

        close_cycle(cycle)

        stipend_high = StipendRecord.objects.get(cycle=cycle, joint_student=js_high)
        stipend_mid = StipendRecord.objects.get(cycle=cycle, joint_student=js_mid)
        stipend_low = StipendRecord.objects.get(cycle=cycle, joint_student=js_low)
        assert stipend_high.grade == "A"
        assert stipend_mid.grade == "B"
        assert stipend_low.grade == "B"

    def test_close_skips_students_without_scores(self):
        """无打分的联培生不会产生补助记录。"""
        cycle = create_cycle()
        js_with = create_joint_student()
        js_without = create_joint_student()
        create_report(joint_student=js_with, status="approved")
        create_report(joint_student=js_without, status="approved")
        create_score(cycle=cycle, joint_student=js_with, expert=create_user(), score=Decimal("90"))

        close_cycle(cycle)

        assert StipendRecord.objects.filter(cycle=cycle, joint_student=js_with).exists()
        assert not StipendRecord.objects.filter(cycle=cycle, joint_student=js_without).exists()

    def test_close_sets_stipend_status_to_pending(self):
        """新建的 StipendRecord status 默认为 pending。"""
        cycle = create_cycle()
        js = create_joint_student()
        create_report(joint_student=js, status="approved")
        create_score(cycle=cycle, joint_student=js, expert=create_user(), score=Decimal("90"))

        close_cycle(cycle)

        stipend = StipendRecord.objects.get(cycle=cycle, joint_student=js)
        assert stipend.status == "pending"

    def test_close_uses_correct_base_amount_by_student_type(self):
        """硕士 3000, 博士 6000。"""
        cycle = create_cycle()
        js_master = create_joint_student(student_type="master")
        js_phd = create_joint_student(student_type="phd")
        for js in [js_master, js_phd]:
            create_report(joint_student=js, status="approved")
        create_score(cycle=cycle, joint_student=js_master, expert=create_user(), score=Decimal("90"))
        create_score(cycle=cycle, joint_student=js_phd, expert=create_user(), score=Decimal("90"))

        close_cycle(cycle)

        master_stipend = StipendRecord.objects.get(cycle=cycle, joint_student=js_master)
        phd_stipend = StipendRecord.objects.get(cycle=cycle, joint_student=js_phd)
        # 2 人 → floor(2*0.4)=0 → 全部 B 档
        assert master_stipend.grade == "B"
        assert phd_stipend.grade == "B"
        assert master_stipend.base_amount == Decimal("3000.00")
        assert phd_stipend.base_amount == Decimal("6000.00")
```

- [ ] **Step 2: 跑测试, 确认 ImportError**

```bash
cd omni_desk_backend && pytest --ds=omni_desk_backend.settings.test omni_desk_backend/joint_students/tests/test_cycle.py -v
```

预期：ImportError。

- [ ] **Step 3: 实现 `services/cycle.py`**

文件 `omni_desk_backend/joint_students/services/cycle.py`：

```python
"""考核批次生命周期管理。"""
import logging
from datetime import date

from django.contrib.auth.models import Group

from joint_students.models import (
    AssessmentCycle,
    MonthlyReport,
    Notification,
    StipendRecord,
)
from joint_students.services.grading import assign_grades
from joint_students.services.stipend import (
    BASE_AMOUNT_MASTER,
    BASE_AMOUNT_PHD,
    GRADE_COEFFICIENT_A,
    GRADE_COEFFICIENT_B,
    compute_attendance_ratio,
    compute_stipend_amount,
)

logger = logging.getLogger(__name__)


def notify_experts(cycle: AssessmentCycle) -> None:
    """批次创建后, 通知 考核专家组 成员。"""
    experts = Group.objects.filter(name="考核专家组").first()
    if not experts:
        logger.warning("考核专家组 不存在, 跳过通知")
        return
    for user in experts.user_set.all():
        Notification.objects.create(
            user=user,
            type="assessment_cycle_started",
            title=f"{cycle.year}-{cycle.month:02d} 考核批次已发起",
            content=f"请于 {cycle.scoring_deadline} 前完成对本月联培生的打分",
            link=f"/joint-students/expert/scoring?cycle={cycle.id}",
        )


def notify_managers_to_review_stipends(cycle: AssessmentCycle) -> None:
    """批次截止后, 通知联培生管理员复核补助。"""
    managers = Group.objects.filter(name="联培生管理员").first()
    if not managers:
        logger.warning("联培生管理员 组不存在, 跳过通知")
        return
    for user in managers.user_set.all():
        Notification.objects.create(
            user=user,
            type="stipend_review_required",
            title=f"{cycle.year}-{cycle.month:02d} 补助待复核",
            content="请进入补助复核页面确认本月补助明细",
            link=f"/joint-students/admin/stipends?cycle={cycle.id}",
        )


def create_cycle(
    year: int, month: int,
    trigger_source: str = AssessmentCycle.TRIGGER_AUTO,
    creator=None,
) -> AssessmentCycle:
    """幂等创建考核批次。"""
    cycle, created = AssessmentCycle.objects.get_or_create(
        year=year, month=month,
        defaults={
            "cycle_start_date": date(year, month, 1),
            "cycle_end_date": date(year, month, 25),
            "scoring_deadline": date(year, month, 28),
            "status": AssessmentCycle.STATUS_COLLECTING,
            "trigger_source": trigger_source,
            "created_by": creator,
        },
    )
    if created:
        logger.info(f"周期 {year}-{month:02d} 创建成功 (id={cycle.id})")
        notify_experts(cycle)
    else:
        logger.info(f"周期 {year}-{month:02d} 已存在 (id={cycle.id}), 跳过")
    return cycle


def close_cycle(cycle: AssessmentCycle) -> None:
    """批次截止: 算 grade + 创建 StipendRecord (status=pending) + 通知管理员复核。"""
    cycle.status = AssessmentCycle.STATUS_CLOSED
    cycle.save(update_fields=["status"])

    grade_records = assign_grades(cycle)
    if not grade_records:
        logger.warning(f"周期 {cycle.year}-{cycle.month:02d} 无任何打分记录, 无补助生成")
        return

    for gr in grade_records:
        from joint_students.models import JointStudent  # 避免循环
        js = JointStudent.objects.get(id=gr["js_id"])
        report = MonthlyReport.objects.get(
            joint_student=js, year=cycle.year, month=cycle.month,
        )
        final_amount = compute_stipend_amount(
            js, gr["grade"],
            report.attendance_days_actual,
            report.attendance_days_expected,
        )
        StipendRecord.objects.create(
            cycle=cycle,
            joint_student=js,
            average_score=gr["avg_score"],
            rank_in_cycle=gr["rank"],
            grade=gr["grade"],
            base_amount=BASE_AMOUNT_MASTER if js.student_type == JointStudent.STUDENT_TYPE_MASTER else BASE_AMOUNT_PHD,
            grade_coefficient=GRADE_COEFFICIENT_A if gr["grade"] == "A" else GRADE_COEFFICIENT_B,
            attendance_ratio=compute_attendance_ratio(
                report.attendance_days_actual, report.attendance_days_expected,
            ),
            final_amount=final_amount,
            status=StipendRecord.STATUS_PENDING,
        )

    notify_managers_to_review_stipends(cycle)
```

- [ ] **Step 4: 跑测试, 确认全部通过**

```bash
cd omni_desk_backend && pytest --ds=omni_desk_backend.settings.test omni_desk_backend/joint_students/tests/test_cycle.py -v
```

预期：所有 test methods PASS。

- [ ] **Step 5: 提交**

```bash
git add omni_desk_backend/joint_students/services/cycle.py omni_desk_backend/joint_students/tests/test_cycle.py
git commit -m "feat(joint-students): 实现批次生命周期 (创建/截止/通知)"
```

---

## Task 7: tasks.py (Celery) + management command

**Files:**
- Create: `omni_desk_backend/joint_students/tasks.py`
- Modify: `omni_desk_backend/omni_desk_backend/celery.py` (Beat schedule)
- Create: `omni_desk_backend/joint_students/management/commands/create_assessment_cycle.py`
- Create: `omni_desk_backend/joint_students/tests/test_tasks.py`

**Interfaces:**
- `check_and_create_assessment_cycle(year=None, month=None, trigger_source='auto', creator=None)` Celery 任务
- Management command: `python manage.py create_assessment_cycle [--year Y --month M --source auto|manual]`

- [ ] **Step 1: 写 Celery 任务测试**

文件 `omni_desk_backend/joint_students/tests/test_tasks.py`：

```python
"""Celery 任务测试。"""
from datetime import date
from unittest.mock import patch

import pytest

from joint_students.models import AssessmentCycle
from joint_students.tasks import check_and_create_assessment_cycle
from joint_students.tests.factories import create_cycle


@pytest.mark.django_db
class TestCheckAndCreateAssessmentCycle:
    def test_create_cycle_for_current_month(self):
        """默认情况下为本月创建批次。"""
        with patch("joint_students.tasks.timezone") as mock_tz:
            from datetime import datetime
            mock_tz.now.return_value = datetime(2026, 7, 15, 10, 0, 0)
            result = check_and_create_assessment_cycle(trigger_source="auto")
        assert result["year"] == 2026
        assert result["month"] == 7
        assert result["status"] == "collecting"
        assert AssessmentCycle.objects.filter(year=2026, month=7).exists()

    def test_create_cycle_idempotent(self):
        """二次创建同月任务不会重复。"""
        create_cycle(year=2026, month=7)
        result = check_and_create_assessment_cycle(
            year=2026, month=7, trigger_source="manual",
        )
        # 第二次调用应该返回已存在, 不创建新行
        assert AssessmentCycle.objects.filter(year=2026, month=7).count() == 1
        assert result["cycle_id"] is not None

    def test_create_cycle_records_trigger_source(self):
        """trigger_source 正确记录。"""
        result = check_and_create_assessment_cycle(
            year=2026, month=8, trigger_source="manual",
        )
        cycle = AssessmentCycle.objects.get(id=result["cycle_id"])
        assert cycle.trigger_source == "manual"
```

- [ ] **Step 2: 跑测试, 确认 ImportError**

```bash
cd omni_desk_backend && pytest --ds=omni_desk_backend.settings.test omni_desk_backend/joint_students/tests/test_tasks.py -v
```

- [ ] **Step 3: 实现 `tasks.py`**

文件 `omni_desk_backend/joint_students/tasks.py`：

```python
"""联培生模块的 Celery 任务。"""
import logging

from celery import shared_task
from django.utils import timezone

from joint_students.models import AssessmentCycle
from joint_students.services.cycle import create_cycle

logger = logging.getLogger(__name__)


@shared_task(name="joint_students.check_and_create_assessment_cycle")
def check_and_create_assessment_cycle(
    year: int = None, month: int = None,
    trigger_source: str = AssessmentCycle.TRIGGER_AUTO,
    creator=None,
) -> dict:
    """检查并创建本月考核批次 (幂等)。

    Celery Beat 每月 1 号 02:00 自动调用 (在 celery.py 配置)。
    联培生管理员也可手动调用 (通过 API 端点)。
    """
    now = timezone.now()
    year = year or now.year
    month = month or now.month

    cycle = create_cycle(year, month, trigger_source=trigger_source, creator=creator)
    return {
        "cycle_id": cycle.id,
        "year": cycle.year,
        "month": cycle.month,
        "status": cycle.status,
        "trigger_source": cycle.trigger_source,
    }
```

- [ ] **Step 4: 创建 management command**

文件 `omni_desk_backend/joint_students/management/commands/create_assessment_cycle.py`：

```python
"""手动创建本月 (或指定年月) 的考核批次。"""
from datetime import date

from django.core.management.base import BaseCommand

from joint_students.models import AssessmentCycle
from joint_students.services.cycle import create_cycle


class Command(BaseCommand):
    help = "手动创建本月 (或指定年月) 的考核批次"

    def add_arguments(self, parser):
        parser.add_argument("--year", type=int, help="年份 (默认今年)")
        parser.add_argument("--month", type=int, help="月份 (默认本月)")
        parser.add_argument(
            "--source", type=str, default="manual",
            choices=[AssessmentCycle.TRIGGER_AUTO, AssessmentCycle.TRIGGER_MANUAL],
            help="触发来源 (默认 manual)",
        )

    def handle(self, *args, **opts):
        year = opts.get("year") or date.today().year
        month = opts.get("month") or date.today().month
        cycle = create_cycle(
            year, month, trigger_source=opts["source"],
        )
        self.stdout.write(self.style.SUCCESS(
            f"批次 {year}-{month:02d} 创建成功: id={cycle.id} status={cycle.status}"
        ))
```

- [ ] **Step 5: 修改 `celery.py` 加入 Beat 调度**

修改 `omni_desk_backend/omni_desk_backend/celery.py`，找到 `app.conf.beat_schedule = {}`（如果不存在则在文件末尾添加）：

```python
from django.conf import settings  # 文件顶部已有则无需重复导入

app.conf.beat_schedule = {
    "create-monthly-assessment-cycle": {
        "task": "joint_students.check_and_create_assessment_cycle",
        "cron": f"0 2 {settings.JOINT_STUDENT_CYCLE_DAY} * *",
        "kwargs": {"trigger_source": "auto"},
    },
}
```

- [ ] **Step 6: 跑测试, 确认全部通过**

```bash
cd omni_desk_backend && pytest --ds=omni_desk_backend.settings.test omni_desk_backend/joint_students/tests/test_tasks.py -v
```

- [ ] **Step 7: 验证 management command 可执行**

```bash
cd omni_desk_backend && python manage.py create_assessment_cycle --year 2026 --month 9 --source manual
```

预期输出：`批次 2026-09 创建成功: id=N status=collecting`

- [ ] **Step 8: 提交**

```bash
git add omni_desk_backend/joint_students/tasks.py omni_desk_backend/joint_students/management/ omni_desk_backend/joint_students/tests/test_tasks.py omni_desk_backend/omni_desk_backend/celery.py
git commit -m "feat(joint-students): 实现 Celery 任务 + management command + Beat 调度"
```

---

## Task 8: permissions.py + serializers.py

**Files:**
- Create: `omni_desk_backend/joint_students/permissions.py`
- Create: `omni_desk_backend/joint_students/serializers.py`

**Interfaces:**
- `IsJointStudentManager` - 联培生管理员 Group 成员
- `IsExpertGroupMember` - 考核专家组 Group 成员
- `IsJointStudentSelfOrManager` - 联培生自己或管理员

- [ ] **Step 1: 创建 `permissions.py`**

文件 `omni_desk_backend/joint_students/permissions.py`：

```python
"""联培生模块的 DRF 权限类。"""
from rest_framework.permissions import SAFE_METHODS, BasePermission

MANAGER_GROUP = "联培生管理员"
EXPERT_GROUP = "考核专家组"


class IsJointStudentManager(BasePermission):
    """必须是 联培生管理员 Group 成员 (写权限)。"""

    def has_permission(self, request, view):
        if not request.user or not request.user.is_authenticated:
            return False
        if request.method in SAFE_METHODS:
            return True
        return request.user.groups.filter(name=MANAGER_GROUP).exists()


class IsExpertGroupMember(BasePermission):
    """必须是 考核专家组 Group 成员。"""

    def has_permission(self, request, view):
        if not request.user or not request.user.is_authenticated:
            return False
        return request.user.groups.filter(name=EXPERT_GROUP).exists()


class IsJointStudentSelfOrManager(BasePermission):
    """联培生自己 或 联培生管理员。"""

    def has_object_permission(self, request, view, obj):
        if not request.user or not request.user.is_authenticated:
            return False
        if request.method in SAFE_METHODS:
            return True
        if request.user.groups.filter(name=MANAGER_GROUP).exists():
            return True
        # obj.joint_student.personnel.user_account → CustomUser
        owner = getattr(obj.joint_student.personnel, "user_account", None)
        return owner == request.user
```

- [ ] **Step 2: 创建 `serializers.py`**

文件 `omni_desk_backend/joint_students/serializers.py`：

```python
"""联培生模块 DRF 序列化器。"""
from decimal import Decimal

from rest_framework import serializers

from joint_students.models import (
    AssessmentCycle,
    ExpertScore,
    JointStudent,
    MonthlyReport,
    StipendRecord,
)


class JointStudentSerializer(serializers.ModelSerializer):
    """联培生列表/详情。"""

    personnel_name = serializers.CharField(source="personnel.name", read_only=True)
    mentor_name = serializers.CharField(source="mentor.name", read_only=True, default=None)
    student_type_display = serializers.CharField(source="get_student_type_display", read_only=True)

    class Meta:
        model = JointStudent
        fields = [
            "id", "personnel", "personnel_name",
            "student_type", "student_type_display",
            "student_id", "enrollment_date", "graduation_date",
            "mentor", "mentor_name", "is_active",
            "created_at", "updated_at",
        ]
        read_only_fields = ["created_at", "updated_at"]


class MonthlyReportSerializer(serializers.ModelSerializer):
    """月度报告。"""

    student_name = serializers.CharField(source="joint_student.personnel.name", read_only=True)
    student_id = serializers.CharField(source="joint_student.student_id", read_only=True)

    class Meta:
        model = MonthlyReport
        fields = [
            "id", "joint_student", "student_name", "student_id",
            "year", "month",
            "work_progress", "work_highlights",
            "attendance_days_actual", "attendance_days_expected", "attendance_notes",
            "status", "submitted_at", "reviewed_at",
            "reviewer", "reviewer_comment",
            "created_at", "updated_at",
        ]
        read_only_fields = [
            "status", "submitted_at", "reviewed_at", "reviewer",
            "created_at", "updated_at",
        ]


class AssessmentCycleSerializer(serializers.ModelSerializer):
    """考核批次。"""

    class Meta:
        model = AssessmentCycle
        fields = [
            "id", "year", "month",
            "cycle_start_date", "cycle_end_date", "scoring_deadline",
            "status", "trigger_source",
            "created_at", "created_by",
        ]
        read_only_fields = [
            "status", "trigger_source", "created_at", "created_by",
        ]


class ExpertScoreSerializer(serializers.ModelSerializer):
    """专家打分。"""

    expert_username = serializers.CharField(source="expert.username", read_only=True)

    class Meta:
        model = ExpertScore
        fields = [
            "id", "cycle", "expert", "expert_username",
            "joint_student", "score", "comment",
            "submitted_at", "is_locked",
        ]
        read_only_fields = [
            "expert", "submitted_at", "is_locked",
        ]


class StipendRecordSerializer(serializers.ModelSerializer):
    """补助记录。"""

    student_name = serializers.CharField(source="joint_student.personnel.name", read_only=True)
    student_id = serializers.CharField(source="joint_student.student_id", read_only=True)
    student_type = serializers.CharField(source="joint_student.student_type", read_only=True)
    grade_display = serializers.CharField(source="get_grade_display", read_only=True)

    class Meta:
        model = StipendRecord
        fields = [
            "id", "cycle", "joint_student",
            "student_name", "student_id", "student_type",
            "average_score", "rank_in_cycle", "grade", "grade_display",
            "base_amount", "grade_coefficient", "attendance_ratio",
            "final_amount", "status",
            "locked_at", "locked_by", "notes",
            "created_at", "updated_at",
        ]
        read_only_fields = [
            "cycle", "joint_student", "average_score", "rank_in_cycle",
            "grade", "base_amount", "grade_coefficient", "attendance_ratio",
            "final_amount", "status", "locked_at", "locked_by",
            "created_at", "updated_at",
        ]
```

- [ ] **Step 3: 验证导入无语法错误**

```bash
cd omni_desk_backend && python manage.py check
```

预期：`System check identified no issues (0 silenced).`

- [ ] **Step 4: 提交**

```bash
git add omni_desk_backend/joint_students/permissions.py omni_desk_backend/joint_students/serializers.py
git commit -m "feat(joint-students): 实现权限类与序列化器"
```

---

## Task 9: views.py + urls.py (核心 ViewSets)

**Files:**
- Create: `omni_desk_backend/joint_students/views.py`
- Create: `omni_desk_backend/joint_students/urls.py`
- Create: `omni_desk_backend/joint_students/tests/test_api.py`
- Modify: `omni_desk_backend/omni_desk_backend/urls.py`

**Interfaces (后续 task 依赖):**
- `JointStudentViewSet` (CRUD + graduate action)
- `MonthlyReportViewSet` (CRUD + submit/approve/reject actions)
- `AssessmentCycleViewSet` (list/retrieve + manual trigger + force-close)
- `ExpertScoreViewSet` (专家打分)
- `StipendRecordViewSet` (list/retrieve + lock action)

- [ ] **Step 1: 写 API 测试**

文件 `omni_desk_backend/joint_students/tests/test_api.py`：

```python
"""API 权限矩阵测试。"""
from datetime import date
from decimal import Decimal

import pytest
from rest_framework.test import APIClient

from joint_students.tests.factories import (
    create_cycle,
    create_group,
    create_joint_student,
    create_personnel,
    create_report,
    create_score,
    create_stipend,
    create_user,
)


# 兼容: 如果 factories.py 没有 create_group, 在 conftest 中定义
try:
    from joint_students.tests.factories import create_group  # noqa
except ImportError:
    from django.contrib.auth.models import Group
    def create_group(name, user=None):
        g, _ = Group.objects.get_or_create(name=name)
        if user:
            user.groups.add(g)
        return g


@pytest.mark.django_db
class TestJointStudentAPI:
    def test_unauthenticated_cannot_list(self):
        client = APIClient()
        resp = client.get("/api/joint-students/students/")
        assert resp.status_code == 401

    def test_manager_can_list(self):
        manager = create_user(username="manager1")
        create_group("联培生管理员", manager)
        client = APIClient()
        client.force_authenticate(manager)
        resp = client.get("/api/joint-students/students/")
        assert resp.status_code == 200

    def test_manager_can_create(self):
        manager = create_user(username="manager1")
        create_group("联培生管理员", manager)
        p = create_personnel()
        client = APIClient()
        client.force_authenticate(manager)
        resp = client.post("/api/joint-students/students/", {
            "personnel": p.id, "student_type": "master",
            "student_id": "S999", "enrollment_date": "2024-09-01",
        }, format="json")
        assert resp.status_code == 201


@pytest.mark.django_db
class TestMonthlyReportAPI:
    def test_student_can_submit_own_report(self):
        user = create_user(username="student1")
        create_group("联培生管理员", user)  # 简化, 用管理员代测
        client = APIClient()
        client.force_authenticate(user)
        js = create_joint_student()
        report = create_report(joint_student=js, year=2026, month=7)
        resp = client.post(f"/api/joint-students/reports/{report.id}/submit/")
        assert resp.status_code == 200
        report.refresh_from_db()
        assert report.status == "submitted"


@pytest.mark.django_db
class TestExpertScoreAPI:
    def test_expert_can_score(self):
        expert = create_user(username="expert1")
        create_group("考核专家组", expert)
        client = APIClient()
        client.force_authenticate(expert)
        cycle = create_cycle()
        js = create_joint_student()
        resp = client.post("/api/joint-students/scores/", {
            "cycle": cycle.id, "joint_student": js.id, "score": "85.00",
            "comment": "工作认真",
        }, format="json")
        assert resp.status_code == 201


@pytest.mark.django_db
class TestCycleAPI:
    def test_manager_can_manual_trigger(self):
        manager = create_user(username="manager1")
        create_group("联培生管理员", manager)
        client = APIClient()
        client.force_authenticate(manager)
        resp = client.post("/api/joint-students/cycles/trigger/", {
            "year": 2026, "month": 11,
        }, format="json")
        # 端点不存在会 404, 但权限通过 -> 期望 200/202 或 404 (URL 未注册)
        # 此测试先验权限位, 实际触发在 task 10 的自定义 action 中
        assert resp.status_code in (200, 202)


@pytest.mark.django_db
class TestStipendLockAPI:
    def test_manager_can_lock(self):
        manager = create_user(username="manager1")
        create_group("联培生管理员", manager)
        client = APIClient()
        client.force_authenticate(manager)
        cycle = create_cycle()
        js = create_joint_student()
        stipend = create_stipend(cycle=cycle, joint_student=js)
        resp = client.post(f"/api/joint-students/stipends/{stipend.id}/lock/")
        assert resp.status_code == 200
        stipend.refresh_from_db()
        assert stipend.status == "locked"
```

- [ ] **Step 2: 实现 `views.py`**

文件 `omni_desk_backend/joint_students/views.py`：

```python
"""联培生模块 DRF ViewSets。"""
from datetime import datetime

from django.shortcuts import get_object_or_404
from rest_framework import status, viewsets
from rest_framework.decorators import action
from rest_framework.response import Response

from joint_students.models import (
    AssessmentCycle,
    ExpertScore,
    JointStudent,
    MonthlyReport,
    StipendRecord,
)
from joint_students.permissions import (
    IsExpertGroupMember,
    IsJointStudentManager,
    IsJointStudentSelfOrManager,
)
from joint_students.serializers import (
    AssessmentCycleSerializer,
    ExpertScoreSerializer,
    JointStudentSerializer,
    MonthlyReportSerializer,
    StipendRecordSerializer,
)


class JointStudentViewSet(viewsets.ModelViewSet):
    """联培生 CRUD (联培生管理员 only)。"""

    queryset = JointStudent.objects.select_related("personnel", "mentor").all()
    serializer_class = JointStudentSerializer
    permission_classes = [IsJointStudentManager]

    @action(detail=True, methods=["post"])
    def graduate(self, request, pk=None):
        """标记毕业: is_active=False + graduation_date=今天。"""
        js = self.get_object()
        js.is_active = False
        js.graduation_date = datetime.now().date()
        js.save(update_fields=["is_active", "graduation_date"])
        return Response({"status": "graduated"})


class MonthlyReportViewSet(viewsets.ModelViewSet):
    """月度报告。"""

    serializer_class = MonthlyReportSerializer
    permission_classes = [IsJointStudentSelfOrManager]

    def get_queryset(self):
        qs = MonthlyReport.objects.select_related("joint_student__personnel").all()
        user = self.request.user
        if user.groups.filter(name="联培生管理员").exists() or user.is_superuser:
            return qs
        # 联培生只看到自己的报告
        own_js_ids = JointStudent.objects.filter(
            personnel__user_account=user
        ).values_list("id", flat=True)
        return qs.filter(joint_student_id__in=own_js_ids)

    @action(detail=True, methods=["post"])
    def submit(self, request, pk=None):
        """联培生提交报告 (draft → submitted)。"""
        report = self.get_object()
        if report.status != MonthlyReport.STATUS_DRAFT:
            return Response({"detail": "只有草稿可提交"}, status=400)
        from django.utils import timezone
        report.status = MonthlyReport.STATUS_SUBMITTED
        report.submitted_at = timezone.now()
        report.save(update_fields=["status", "submitted_at"])
        return Response({"status": "submitted"})

    @action(detail=True, methods=["post"])
    def approve(self, request, pk=None):
        """联培生管理员通过 (submitted → approved)。"""
        report = self.get_object()
        if report.status != MonthlyReport.STATUS_SUBMITTED:
            return Response({"detail": "只有已提交可审核"}, status=400)
        from django.utils import timezone
        report.status = MonthlyReport.STATUS_APPROVED
        report.reviewed_at = timezone.now()
        report.reviewer = request.user
        report.save(update_fields=["status", "reviewed_at", "reviewer"])
        return Response({"status": "approved"})

    @action(detail=True, methods=["post"])
    def reject(self, request, pk=None):
        """联培生管理员驳回 (submitted → rejected)。需 reviewer_comment。"""
        report = self.get_object()
        if report.status != MonthlyReport.STATUS_SUBMITTED:
            return Response({"detail": "只有已提交可驳回"}, status=400)
        comment = request.data.get("reviewer_comment", "").strip()
        if not comment:
            return Response({"detail": "驳回必须填写审核意见"}, status=400)
        from django.utils import timezone
        report.status = MonthlyReport.STATUS_REJECTED
        report.reviewed_at = timezone.now()
        report.reviewer = request.user
        report.reviewer_comment = comment
        report.save(update_fields=[
            "status", "reviewed_at", "reviewer", "reviewer_comment",
        ])
        return Response({"status": "rejected"})


class AssessmentCycleViewSet(viewsets.ModelViewSet):
    """考核批次。"""

    queryset = AssessmentCycle.objects.all()
    serializer_class = AssessmentCycleSerializer
    permission_classes = [IsJointStudentManager]
    http_method_names = ["get", "post"]  # 不允许 update/delete

    @action(detail=False, methods=["post"], url_path="trigger")
    def trigger(self, request):
        """联培生管理员手动提前触发本月批次。"""
        from joint_students.services.cycle import create_cycle
        year = int(request.data.get("year") or datetime.now().year)
        month = int(request.data.get("month") or datetime.now().month)
        cycle = create_cycle(
            year, month,
            trigger_source=AssessmentCycle.TRIGGER_MANUAL,
            creator=request.user,
        )
        return Response(AssessmentCycleSerializer(cycle).data, status=201)

    @action(detail=True, methods=["post"])
    def force_close(self, request, pk=None):
        """admin 强制截止 (collecting → closed)。"""
        cycle = self.get_object()
        if cycle.status != AssessmentCycle.STATUS_COLLECTING:
            return Response({"detail": "仅 collecting 状态可强制截止"}, status=400)
        from joint_students.services.cycle import close_cycle
        close_cycle(cycle)
        return Response({"status": "closed"})


class ExpertScoreViewSet(viewsets.ModelViewSet):
    """专家打分。"""

    serializer_class = ExpertScoreSerializer
    permission_classes = [IsExpertGroupMember]

    def get_queryset(self):
        # 专家只看到自己的打分
        return ExpertScore.objects.filter(expert=self.request.user).select_related(
            "expert", "joint_student__personnel", "cycle",
        )

    def perform_create(self, serializer):
        from django.utils import timezone
        serializer.save(
            expert=self.request.user,
            submitted_at=timezone.now(),
            is_locked=True,
        )

    @action(detail=True, methods=["post"], url_path="unlock")
    def unlock(self, request, pk=None):
        """仅 admin 可解锁。"""
        if not request.user.is_superuser:
            return Response({"detail": "仅 admin 可解锁"}, status=403)
        score = self.get_object()
        score.is_locked = False
        score.save(update_fields=["is_locked"])
        return Response({"status": "unlocked"})


class StipendRecordViewSet(viewsets.ReadOnlyModelViewSet):
    """补助记录 (只读 + lock action)。"""

    queryset = StipendRecord.objects.select_related(
        "joint_student__personnel", "cycle",
    ).all()
    serializer_class = StipendRecordSerializer
    permission_classes = [IsJointStudentManager]

    def get_queryset(self):
        qs = super().get_queryset()
        user = self.request.user
        if user.groups.filter(name="联培生管理员").exists() or user.is_superuser:
            return qs
        # 联培生只能看到已锁定 且 是自己的
        own_js_ids = JointStudent.objects.filter(
            personnel__user_account=user
        ).values_list("id", flat=True)
        return qs.filter(joint_student_id__in=own_js_ids, status=StipendRecord.STATUS_LOCKED)

    @action(detail=True, methods=["post"])
    def lock(self, request, pk=None):
        """联培生管理员复核 + 锁定 (pending → locked, 联培生可见)。"""
        stipend = self.get_object()
        if stipend.status != StipendRecord.STATUS_PENDING:
            return Response({"detail": "仅 pending 可锁定"}, status=400)
        from django.utils import timezone
        stipend.status = StipendRecord.STATUS_LOCKED
        stipend.locked_at = timezone.now()
        stipend.locked_by = request.user
        stipend.notes = request.data.get("notes", stipend.notes)
        stipend.save(update_fields=["status", "locked_at", "locked_by", "notes"])
        # 通知联培生
        owner = getattr(stipend.joint_student.personnel, "user_account", None)
        if owner:
            from joint_students.models import Notification
            Notification.objects.create(
                user=owner,
                type="stipend_locked",
                title=f"您 {stipend.cycle.year}-{stipend.cycle.month:02d} 的补助已锁定",
                content=f"本月补助: {stipend.final_amount} 元 ({stipend.get_grade_display()})",
                link="/joint-students/student/stipends",
            )
        return Response({"status": "locked"})
```

- [ ] **Step 3: 创建 `urls.py`**

文件 `omni_desk_backend/joint_students/urls.py`：

```python
from rest_framework.routers import DefaultRouter

from .views import (
    AssessmentCycleViewSet,
    ExpertScoreViewSet,
    JointStudentViewSet,
    MonthlyReportViewSet,
    StipendRecordViewSet,
)

router = DefaultRouter()
router.register(r"students", JointStudentViewSet, basename="joint-student")
router.register(r"reports", MonthlyReportViewSet, basename="monthly-report")
router.register(r"cycles", AssessmentCycleViewSet, basename="assessment-cycle")
router.register(r"scores", ExpertScoreViewSet, basename="expert-score")
router.register(r"stipends", StipendRecordViewSet, basename="stipend")

urlpatterns = router.urls
```

- [ ] **Step 4: 在项目 `urls.py` 注册**

修改 `omni_desk_backend/omni_desk_backend/urls.py`，在 `urlpatterns` 中追加：

```python
    path("api/joint-students/", include("joint_students.urls")),
```

- [ ] **Step 5: 跑测试**

```bash
cd omni_desk_backend && pytest --ds=omni_desk_backend.settings.test omni_desk_backend/joint_students/tests/test_api.py -v
```

预期：所有 test methods PASS（部分可能因 URL/字段差异失败, 自行调整）。

- [ ] **Step 6: 提交**

```bash
git add omni_desk_backend/joint_students/views.py omni_desk_backend/joint_students/urls.py omni_desk_backend/joint_students/tests/test_api.py omni_desk_backend/omni_desk_backend/urls.py
git commit -m "feat(joint-students): 实现 ViewSets + 路由 + API 权限测试"
```

---

## Task 10: 前端 API client (axios)

**Files:**
- Create: `omni_desk_frontend/src/features/joint-students/api/client.js`
- Create: `omni_desk_frontend/src/features/joint-students/api/students.js`
- Create: `omni_desk_frontend/src/features/joint-students/api/reports.js`
- Create: `omni_desk_frontend/src/features/joint-students/api/cycles.js`
- Create: `omni_desk_frontend/src/features/joint-students/api/scores.js`
- Create: `omni_desk_frontend/src/features/joint-students/api/stipends.js`
- Create: `omni_desk_frontend/src/features/joint-students/__tests__/api/students.test.js`

- [ ] **Step 1: 创建基础 axios client**

文件 `omni_desk_frontend/src/features/joint-students/api/client.js`：

```javascript
import axios from 'axios';

const apiClient = axios.create({
  baseURL: '/api/joint-students/',
  timeout: 15000,
  headers: { 'Content-Type': 'application/json' },
});

// JWT 拦截器 (与现有 axiosConfig.js 保持一致)
apiClient.interceptors.request.use((config) => {
  const token = localStorage.getItem('access_token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

export default apiClient;
```

- [ ] **Step 2: 创建 5 个 API 模块**

文件 `omni_desk_frontend/src/features/joint-students/api/students.js`：

```javascript
import client from './client';

export const listStudents = (params) => client.get('students/', { params });
export const getStudent = (id) => client.get(`students/${id}/`);
export const createStudent = (data) => client.post('students/', data);
export const updateStudent = (id, data) => client.patch(`students/${id}/`, data);
export const graduateStudent = (id) => client.post(`students/${id}/graduate/`);
```

文件 `omni_desk_frontend/src/features/joint-students/api/reports.js`：

```javascript
import client from './client';

export const listReports = (params) => client.get('reports/', { params });
export const getReport = (id) => client.get(`reports/${id}/`);
export const createReport = (data) => client.post('reports/', data);
export const updateReport = (id, data) => client.patch(`reports/${id}/`, data);
export const submitReport = (id) => client.post(`reports/${id}/submit/`);
export const approveReport = (id) => client.post(`reports/${id}/approve/`);
export const rejectReport = (id, comment) =>
  client.post(`reports/${id}/reject/`, { reviewer_comment: comment });
```

文件 `omni_desk_frontend/src/features/joint-students/api/cycles.js`：

```javascript
import client from './client';

export const listCycles = () => client.get('cycles/');
export const getCycle = (id) => client.get(`cycles/${id}/`);
export const triggerCycle = (year, month) =>
  client.post('cycles/trigger/', { year, month });
export const forceCloseCycle = (id) => client.post(`cycles/${id}/force_close/`);
```

文件 `omni_desk_frontend/src/features/joint-students/api/scores.js`：

```javascript
import client from './client';

export const createScore = (data) => client.post('scores/', data);
export const unlockScore = (id) => client.post(`scores/${id}/unlock/`);
```

文件 `omni_desk_frontend/src/features/joint-students/api/stipends.js`：

```javascript
import client from './client';

export const listStipends = (params) => client.get('stipends/', { params });
export const getStipend = (id) => client.get(`stipends/${id}/`);
export const lockStipend = (id, notes) =>
  client.post(`stipends/${id}/lock/`, { notes });
```

- [ ] **Step 3: 写 API client 测试**

文件 `omni_desk_frontend/src/features/joint-students/__tests__/api/students.test.js`：

```javascript
import { listStudents, createStudent, graduateStudent } from '../../api/students';
import client from '../../api/client';

jest.mock('../../api/client');

describe('students API', () => {
  it('listStudents calls GET students/', async () => {
    client.get.mockResolvedValue({ data: [] });
    await listStudents({ active: true });
    expect(client.get).toHaveBeenCalledWith('students/', { params: { active: true } });
  });

  it('createStudent calls POST students/', async () => {
    client.post.mockResolvedValue({ data: { id: 1 } });
    const payload = { personnel: 5, student_type: 'master', student_id: 'S001' };
    await createStudent(payload);
    expect(client.post).toHaveBeenCalledWith('students/', payload);
  });

  it('graduateStudent calls POST students/:id/graduate/', async () => {
    client.post.mockResolvedValue({ data: { status: 'graduated' } });
    await graduateStudent(7);
    expect(client.post).toHaveBeenCalledWith('students/7/graduate/');
  });
});
```

- [ ] **Step 4: 跑测试**

```bash
cd omni_desk_frontend && npm test -- --testPathPattern=joint-students
```

预期：3 个测试 PASS。

- [ ] **Step 5: 提交**

```bash
git add omni_desk_frontend/src/features/joint-students/api/ omni_desk_frontend/src/features/joint-students/__tests__/
git commit -m "feat(joint-students-frontend): 实现 5 个 API client 模块与测试"
```

---

## Task 11: 前端关键组件 (GradeBadge + StipendBreakdown)

**Files:**
- Create: `omni_desk_frontend/src/features/joint-students/components/GradeBadge.jsx`
- Create: `omni_desk_frontend/src/features/joint-students/components/StipendBreakdown.jsx`
- Create: `omni_desk_frontend/src/features/joint-students/__tests__/GradeBadge.test.jsx`
- Create: `omni_desk_frontend/src/features/joint-students/__tests__/StipendBreakdown.test.jsx`

- [ ] **Step 1: 实现 GradeBadge**

文件 `omni_desk_frontend/src/features/joint-students/components/GradeBadge.jsx`：

```javascript
import React from 'react';
import { Tag } from 'antd';

/**
 * A/B 档徽章
 * @param {{grade: 'A'|'B'}} props
 */
const GradeBadge = ({ grade }) => {
  const isA = grade === 'A';
  return (
    <Tag color={isA ? 'green' : 'default'} style={{ fontWeight: 'bold' }}>
      {isA ? 'A 档' : 'B 档'}
    </Tag>
  );
};

export default GradeBadge;
```

- [ ] **Step 2: 实现 StipendBreakdown**

文件 `omni_desk_frontend/src/features/joint-students/components/StipendBreakdown.jsx`：

```javascript
import React from 'react';
import { Descriptions } from 'antd';
import GradeBadge from './GradeBadge';

/**
 * 补助明细展示 (基本额度 × 档次系数 × 出勤比 = 最终)
 * @param {{stipend: object}} props - stipend 来自 API
 */
const StipendBreakdown = ({ stipend }) => {
  const items = [
    { key: 'grade', label: '档次', children: <GradeBadge grade={stipend.grade} /> },
    { key: 'average', label: '平均分', children: stipend.average_score },
    { key: 'base', label: '基本额度', children: `${stipend.base_amount} 元` },
    { key: 'coef', label: '档次系数', children: stipend.grade_coefficient },
    { key: 'ratio', label: '出勤比', children: stipend.attendance_ratio },
    { key: 'final', label: '最终金额', children: (
        <span style={{ color: '#52c41a', fontWeight: 'bold', fontSize: 16 }}>
          {stipend.final_amount} 元
        </span>
      ),
    },
  ];
  return <Descriptions items={items} column={1} bordered size="small" />;
};

export default StipendBreakdown;
```

- [ ] **Step 3: 写组件测试**

文件 `omni_desk_frontend/src/features/joint-students/__tests__/GradeBadge.test.jsx`：

```javascript
import React from 'react';
import { render, screen } from '@testing-library/react';
import GradeBadge from '../components/GradeBadge';

describe('GradeBadge', () => {
  it('renders A grade in green', () => {
    render(<GradeBadge grade="A" />);
    expect(screen.getByText('A 档')).toBeInTheDocument();
  });
  it('renders B grade in default color', () => {
    render(<GradeBadge grade="B" />);
    expect(screen.getByText('B 档')).toBeInTheDocument();
  });
});
```

文件 `omni_desk_frontend/src/features/joint-students/__tests__/StipendBreakdown.test.jsx`：

```javascript
import React from 'react';
import { render, screen } from '@testing-library/react';
import StipendBreakdown from '../components/StipendBreakdown';

const mockStipend = {
  grade: 'A',
  average_score: '95.00',
  base_amount: '6000.00',
  grade_coefficient: '1.00',
  attendance_ratio: '1.00',
  final_amount: '6000.00',
};

describe('StipendBreakdown', () => {
  it('renders all fields', () => {
    render(<StipendBreakdown stipend={mockStipend} />);
    expect(screen.getByText('A 档')).toBeInTheDocument();
    expect(screen.getByText('95.00')).toBeInTheDocument();
    expect(screen.getByText('6000.00 元')).toBeInTheDocument();
    expect(screen.getByText('6000.00 元').length).toBeGreaterThan(0);
  });
});
```

- [ ] **Step 4: 跑测试**

```bash
cd omni_desk_frontend && npm test -- --testPathPattern=joint-students
```

预期：5 个测试 PASS。

- [ ] **Step 5: 提交**

```bash
git add omni_desk_frontend/src/features/joint-students/components/ omni_desk_frontend/src/features/joint-students/__tests__/
git commit -m "feat(joint-students-frontend): 实现 GradeBadge 与 StipendBreakdown 组件"
```

---

## Task 12: 前端 4 个角色页面 (列表/详情骨架)

**Files:**
- Create: `omni_desk_frontend/src/features/joint-students/pages/admin/StudentListPage.jsx`
- Create: `omni_desk_frontend/src/features/joint-students/pages/admin/StudentEditPage.jsx`
- Create: `omni_desk_frontend/src/features/joint-students/pages/expert/ExpertScoringPage.jsx`
- Create: `omni_desk_frontend/src/features/joint-students/pages/student/MyReportsPage.jsx`
- Create: `omni_desk_frontend/src/features/joint-students/pages/student/ReportSubmitPage.jsx`
- Create: `omni_desk_frontend/src/features/joint-students/pages/admin/StipendReviewPage.jsx`
- Create: `omni_desk_frontend/src/features/joint-students/pages/mentor/MentorOverviewPage.jsx`

- [ ] **Step 1: 实现 StudentListPage (联培生管理员)**

文件 `omni_desk_frontend/src/features/joint-students/pages/admin/StudentListPage.jsx`：

```javascript
import React from 'react';
import { Table, Button, Space } from 'antd';
import { useQuery } from '@tanstack/react-query';
import { Link } from 'react-router-dom';
import { listStudents } from '../../api/students';

const StudentListPage = () => {
  const { data, isLoading } = useQuery({
    queryKey: ['students'],
    queryFn: () => listStudents().then((r) => r.data),
  });

  const columns = [
    { title: '学号', dataIndex: 'student_id', key: 'student_id' },
    { title: '姓名', dataIndex: 'personnel_name', key: 'personnel_name' },
    { title: '类型', dataIndex: 'student_type_display', key: 'student_type' },
    { title: '入学日期', dataIndex: 'enrollment_date', key: 'enrollment_date' },
    { title: '导师', dataIndex: 'mentor_name', key: 'mentor_name' },
    {
      title: '状态',
      dataIndex: 'is_active',
      key: 'is_active',
      render: (v) => (v ? '在读' : '已毕业'),
    },
    {
      title: '操作',
      key: 'actions',
      render: (_, record) => (
        <Space>
          <Link to={`/joint-students/admin/students/${record.id}`}>详情</Link>
        </Space>
      ),
    },
  ];

  return (
    <div style={{ padding: 24 }}>
      <h2>联培生管理</h2>
      <Link to="/joint-students/admin/students/new">
        <Button type="primary" style={{ marginBottom: 16 }}>新建联培生</Button>
      </Link>
      <Table
        rowKey="id"
        loading={isLoading}
        dataSource={data || []}
        columns={columns}
      />
    </div>
  );
};

export default StudentListPage;
```

- [ ] **Step 2: 实现 StudentEditPage (创建联培生)**

文件 `omni_desk_frontend/src/features/joint-students/pages/admin/StudentEditPage.jsx`：

```javascript
import React, { useState } from 'react';
import { Form, Input, Select, DatePicker, Button, message } from 'antd';
import { useNavigate } from 'react-router-dom';
import { createStudent } from '../../api/students';
import { listPersonnelPool } from '../../api/personnelPool';

const StudentEditPage = () => {
  const [form] = Form.useForm();
  const navigate = useNavigate();
  const [submitting, setSubmitting] = useState(false);
  const { data: pool } = useQuery({
    queryKey: ['personnel-pool'],
    queryFn: () => listPersonnelPool().then((r) => r.data),
  });

  const onFinish = async (values) => {
    setSubmitting(true);
    try {
      await createStudent({
        ...values,
        enrollment_date: values.enrollment_date.format('YYYY-MM-DD'),
      });
      message.success('创建成功');
      navigate('/joint-students/admin/students');
    } catch (err) {
      message.error(`创建失败: ${err.response?.data?.detail || err.message}`);
    } finally {
      setSubmitting(false);
    }
  };

  return (
    <div style={{ padding: 24 }}>
      <h2>新建联培生</h2>
      <Form form={form} layout="vertical" onFinish={onFinish} style={{ maxWidth: 600 }}>
        <Form.Item name="personnel" label="关联人员" rules={[{ required: true }]}>
          <Select
            showSearch
            placeholder="选择 Personnel"
            options={(pool || []).map((p) => ({
              value: p.id,
              label: `${p.name} (${p.department || '未分配部门'})`,
            }))}
            filterOption={(input, option) =>
              option.label.toLowerCase().includes(input.toLowerCase())
            }
          />
        </Form.Item>
        <Form.Item name="student_type" label="培养类型" rules={[{ required: true }]}>
          <Select options={[
            { value: 'master', label: '硕士' },
            { value: 'phd', label: '博士' },
          ]} />
        </Form.Item>
        <Form.Item name="student_id" label="学号" rules={[{ required: true }]}>
          <Input />
        </Form.Item>
        <Form.Item name="enrollment_date" label="入学日期" rules={[{ required: true }]}>
          <DatePicker style={{ width: '100%' }} />
        </Form.Item>
        <Form.Item>
          <Button type="primary" htmlType="submit" loading={submitting}>提交</Button>
        </Form.Item>
      </Form>
    </div>
  );
};

export default StudentEditPage;
```

- [ ] **Step 3: 实现 personnel-pool API helper**

文件 `omni_desk_frontend/src/features/joint-students/api/personnelPool.js`（新增独立模块，因为 personnel-pool 端点不在 joint-students API 客户端）：

```javascript
import axios from 'axios';

const client = axios.create({ baseURL: '/api/', timeout: 15000 });

client.interceptors.request.use((config) => {
  const token = localStorage.getItem('access_token');
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

export const listPersonnelPool = (params) =>
  client.get('joint-students/personnel-pool/', { params });
```

- [ ] **Step 4: 实现 ExpertScoringPage (专家组打分)**

文件 `omni_desk_frontend/src/features/joint-students/pages/expert/ExpertScoringPage.jsx`：

```javascript
import React, { useState } from 'react';
import { Table, InputNumber, Button, message, Form } from 'antd';
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { listCycles } from '../../api/cycles';
import { listReports } from '../../api/reports';
import { createScore } from '../../api/scores';

const ExpertScoringPage = () => {
  const qc = useQueryClient();
  const [selectedCycle, setSelectedCycle] = useState(null);
  const { data: cycles } = useQuery({
    queryKey: ['cycles'],
    queryFn: () => listCycles().then((r) => r.data),
  });
  const { data: reports } = useQuery({
    queryKey: ['reports', selectedCycle?.id],
    queryFn: () => listReports({
      year: selectedCycle.year, month: selectedCycle.month, status: 'approved',
    }).then((r) => r.data),
    enabled: !!selectedCycle,
  });

  const mutation = useMutation({
    mutationFn: createScore,
    onSuccess: () => {
      message.success('打分成功');
      qc.invalidateQueries(['reports']);
    },
    onError: (e) => message.error(`打分失败: ${e.response?.data?.detail || e.message}`),
  });

  const columns = [
    { title: '学号', dataIndex: ['student_id'] },
    { title: '姓名', dataIndex: ['student_name'] },
    {
      title: '打分',
      key: 'score',
      render: (_, record) => (
        <InputNumber
          min={0} max={100} precision={2} step={1}
          onChange={(v) => record._score = v}
        />
      ),
    },
    {
      title: '操作',
      key: 'submit',
      render: (_, record) => (
        <Button
          type="primary"
          onClick={() => mutation.mutate({
            cycle: selectedCycle.id,
            joint_student: record.joint_student,
            score: record._score || 85,
          })}
        >提交</Button>
      ),
    },
  ];

  return (
    <div style={{ padding: 24 }}>
      <h2>专家打分</h2>
      <Form.Item label="选择考核批次">
        <select onChange={(e) => {
          const c = cycles?.find((x) => x.id === Number(e.target.value));
          setSelectedCycle(c);
        }} value={selectedCycle?.id || ''}>
          <option value="">-- 请选择 --</option>
          {(cycles || []).map((c) => (
            <option key={c.id} value={c.id}>{c.year}-{String(c.month).padStart(2, '0')}</option>
          ))}
        </select>
      </Form.Item>
      {selectedCycle && (
        <Table rowKey="id" dataSource={reports || []} columns={columns} />
      )}
    </div>
  );
};

export default ExpertScoringPage;
```

- [ ] **Step 5: 实现 MyReportsPage + ReportSubmitPage (联培生自助)**

文件 `omni_desk_frontend/src/features/joint-students/pages/student/MyReportsPage.jsx`：

```javascript
import React from 'react';
import { Table, Button } from 'antd';
import { useQuery } from '@tanstack/react-query';
import { Link } from 'react-router-dom';
import { listReports } from '../../api/reports';

const MyReportsPage = () => {
  const { data, isLoading } = useQuery({
    queryKey: ['my-reports'],
    queryFn: () => listReports().then((r) => r.data),
  });

  const columns = [
    { title: '年月', key: 'ym', render: (_, r) => `${r.year}-${String(r.month).padStart(2, '0')}` },
    { title: '工作亮点', dataIndex: 'work_highlights', ellipsis: true },
    { title: '出勤 (实/应)', key: 'att', render: (_, r) => `${r.attendance_days_actual}/${r.attendance_days_expected}` },
    { title: '状态', dataIndex: 'status' },
  ];

  return (
    <div style={{ padding: 24 }}>
      <h2>我的月度报告</h2>
      <Link to="/joint-students/student/reports/new">
        <Button type="primary" style={{ marginBottom: 16 }}>填报本月报告</Button>
      </Link>
      <Table rowKey="id" loading={isLoading} dataSource={data || []} columns={columns} />
    </div>
  );
};

export default MyReportsPage;
```

文件 `omni_desk_frontend/src/features/joint-students/pages/student/ReportSubmitPage.jsx`：

```javascript
import React, { useState } from 'react';
import { Form, Input, InputNumber, Button, message, DatePicker } from 'antd';
import { useNavigate } from 'react-router-dom';
import { createReport, submitReport } from '../../api/reports';

const ReportSubmitPage = () => {
  const [form] = Form.useForm();
  const navigate = useNavigate();
  const [submitting, setSubmitting] = useState(false);

  const onFinish = async (values) => {
    setSubmitting(true);
    try {
      const [year, month] = values.ym.split('-').map(Number);
      const created = await createReport({
        ...values,
        year, month,
      });
      await submitReport(created.data.id);
      message.success('提交成功');
      navigate('/joint-students/student/reports');
    } catch (e) {
      message.error(`提交失败: ${e.response?.data?.detail || e.message}`);
    } finally {
      setSubmitting(false);
    }
  };

  return (
    <div style={{ padding: 24 }}>
      <h2>填报月度报告</h2>
      <Form form={form} layout="vertical" onFinish={onFinish} style={{ maxWidth: 600 }}>
        <Form.Item name="ym" label="年月" rules={[{ required: true }]}>
          <DatePicker picker="month" style={{ width: '100%' }} />
        </Form.Item>
        <Form.Item name="work_progress" label="工作进展" rules={[{ required: true }]}>
          <Input.TextArea rows={4} />
        </Form.Item>
        <Form.Item name="work_highlights" label="工作亮点" rules={[{ required: true }]}>
          <Input.TextArea rows={3} />
        </Form.Item>
        <Form.Item name="attendance_days_actual" label="实际出勤天数" rules={[{ required: true }]}>
          <InputNumber min={0} max={31} precision={1} step={0.5} style={{ width: '100%' }} />
        </Form.Item>
        <Form.Item name="attendance_days_expected" label="应出勤天数" initialValue={22}>
          <InputNumber min={0} max={31} precision={1} style={{ width: '100%' }} />
        </Form.Item>
        <Form.Item name="attendance_notes" label="出勤说明">
          <Input.TextArea rows={2} />
        </Form.Item>
        <Form.Item>
          <Button type="primary" htmlType="submit" loading={submitting}>提交</Button>
        </Form.Item>
      </Form>
    </div>
  );
};

export default ReportSubmitPage;
```

- [ ] **Step 6: 实现 StipendReviewPage + MentorOverviewPage**

文件 `omni_desk_frontend/src/features/joint-students/pages/admin/StipendReviewPage.jsx`：

```javascript
import React from 'react';
import { Table, Button, Modal } from 'antd';
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { listStipends, lockStipend } from '../../api/stipends';
import StipendBreakdown from '../../components/StipendBreakdown';

const StipendReviewPage = () => {
  const qc = useQueryClient();
  const { data, isLoading } = useQuery({
    queryKey: ['stipends'],
    queryFn: () => listStipends().then((r) => r.data),
  });
  const mutation = useMutation({
    mutationFn: ({ id, notes }) => lockStipend(id, notes),
    onSuccess: () => {
      Modal.success({ title: '锁定成功' });
      qc.invalidateQueries(['stipends']);
    },
  });

  const columns = [
    { title: '学号', dataIndex: 'student_id' },
    { title: '姓名', dataIndex: 'student_name' },
    { title: '平均分', dataIndex: 'average_score' },
    { title: '档次', dataIndex: 'grade' },
    { title: '金额', dataIndex: 'final_amount' },
    { title: '状态', dataIndex: 'status' },
    {
      title: '操作',
      key: 'lock',
      render: (_, record) =>
        record.status === 'pending' ? (
          <Button type="primary" onClick={() => mutation.mutate({ id: record.id })}>
            复核锁定
          </Button>
        ) : (
          <span>已锁定</span>
        ),
    },
  ];

  return (
    <div style={{ padding: 24 }}>
      <h2>补助复核</h2>
      <Table
        rowKey="id"
        loading={isLoading}
        dataSource={data || []}
        columns={columns}
        expandable={{
          expandedRowRender: (record) => <StipendBreakdown stipend={record} />,
        }}
      />
    </div>
  );
};

export default StipendReviewPage;
```

文件 `omni_desk_frontend/src/features/joint-students/pages/mentor/MentorOverviewPage.jsx`：

```javascript
import React from 'react';
import { Table } from 'antd';
import { useQuery } from '@tanstack/react-query';
import axios from 'axios';
import { listReports } from '../../api/reports';

const MentorOverviewPage = () => {
  const { data, isLoading } = useQuery({
    queryKey: ['mentor-students'],
    queryFn: async () => {
      const token = localStorage.getItem('access_token');
      const resp = await axios.get('/api/joint-students/mentor/students/', {
        headers: { Authorization: `Bearer ${token}` },
      });
      return resp.data;
    },
  });

  const columns = [
    { title: '学号', dataIndex: 'student_id' },
    { title: '姓名', dataIndex: 'personnel_name' },
    { title: '类型', dataIndex: 'student_type_display' },
    { title: '入学日期', dataIndex: 'enrollment_date' },
  ];

  return (
    <div style={{ padding: 24 }}>
      <h2>我指导的联培生</h2>
      <Table rowKey="id" loading={isLoading} dataSource={data || []} columns={columns} />
    </div>
  );
};

export default MentorOverviewPage;
```

- [ ] **Step 7: 验证导入无语法错误**

```bash
cd omni_desk_frontend && npx eslint src/features/joint-students/
```

预期：无错误。

- [ ] **Step 8: 提交**

```bash
git add omni_desk_frontend/src/features/joint-students/pages/ omni_desk_frontend/src/features/joint-students/api/personnelPool.js
git commit -m "feat(joint-students-frontend): 实现 7 个核心页面"
```

---

## Task 13: 前端路由注册 + 导航菜单

**Files:**
- Modify: `omni_desk_frontend/src/routes/index.jsx`
- Modify: `omni_desk_frontend/src/AdminAppWrapper.jsx` (导航菜单)

- [ ] **Step 1: 注册 12 个路由**

修改 `omni_desk_frontend/src/routes/index.jsx`，在文件顶部添加 import：

```javascript
const StudentListPage = lazy(() => import('../features/joint-students/pages/admin/StudentListPage'));
const StudentEditPage = lazy(() => import('../features/joint-students/pages/admin/StudentEditPage'));
const StudentDetailPage = lazy(() => import('../features/joint-students/pages/admin/StudentDetailPage'));
const ReportReviewPage = lazy(() => import('../features/joint-students/pages/admin/ReportReviewPage'));
const CycleManagementPage = lazy(() => import('../features/joint-students/pages/admin/CycleManagementPage'));
const CycleDetailPage = lazy(() => import('../features/joint-students/pages/admin/CycleDetailPage'));
const StipendReviewPage = lazy(() => import('../features/joint-students/pages/admin/StipendReviewPage'));
const ExpertScoringPage = lazy(() => import('../features/joint-students/pages/expert/ExpertScoringPage'));
const MyReportsPage = lazy(() => import('../features/joint-students/pages/student/MyReportsPage'));
const ReportSubmitPage = lazy(() => import('../features/joint-students/pages/student/ReportSubmitPage'));
const MyStipendsPage = lazy(() => import('../features/joint-students/pages/student/MyStipendsPage'));
const MentorOverviewPage = lazy(() => import('../features/joint-students/pages/mentor/MentorOverviewPage'));
```

在 `<Router>` 内部 children 路由追加：

```jsx
<Route path="/joint-students/admin/students" element={<ProtectedRoute pageName="联培生-管理员首页"><StudentListPage /></ProtectedRoute>} />
<Route path="/joint-students/admin/students/new" element={<ProtectedRoute pageName="联培生-创建联培生"><StudentEditPage /></ProtectedRoute>} />
<Route path="/joint-students/admin/students/:id" element={<ProtectedRoute pageName="联培生-联培生详情"><StudentDetailPage /></ProtectedRoute>} />
<Route path="/joint-students/admin/reports" element={<ProtectedRoute pageName="联培生-报告审核"><ReportReviewPage /></ProtectedRoute>} />
<Route path="/joint-students/admin/cycles" element={<ProtectedRoute pageName="联培生-批次管理"><CycleManagementPage /></ProtectedRoute>} />
<Route path="/joint-students/admin/cycles/:id" element={<ProtectedRoute pageName="联培生-批次详情"><CycleDetailPage /></ProtectedRoute>} />
<Route path="/joint-students/admin/stipends" element={<ProtectedRoute pageName="联培生-补助复核"><StipendReviewPage /></ProtectedRoute>} />
<Route path="/joint-students/expert/scoring" element={<ProtectedRoute pageName="联培生-专家打分"><ExpertScoringPage /></ProtectedRoute>} />
<Route path="/joint-students/student/reports" element={<ProtectedRoute pageName="联培生-我的报告"><MyReportsPage /></ProtectedRoute>} />
<Route path="/joint-students/student/reports/new" element={<ProtectedRoute pageName="联培生-填报报告"><ReportSubmitPage /></ProtectedRoute>} />
<Route path="/joint-students/student/stipends" element={<ProtectedRoute pageName="联培生-我的补助"><MyStipendsPage /></ProtectedRoute>} />
<Route path="/joint-students/mentor/overview" element={<ProtectedRoute pageName="联培生-导师视图"><MentorOverviewPage /></ProtectedRoute>} />
```

- [ ] **Step 2: 添加导航菜单项**

修改 `omni_desk_frontend/src/AdminAppWrapper.jsx` 或导航菜单文件，在合适位置（如"控制面板"或"人员"附近）加入：

```jsx
<Menu.Item key="joint-students" icon={<TeamOutlined />}>
  <Link to="/joint-students/admin/students">联培生管理</Link>
</Menu.Item>
```

- [ ] **Step 3: 验证路由可访问（开发服务器）**

```bash
cd omni_desk_frontend && npm run dev
```

浏览器访问 `http://localhost:3000/joint-students/admin/students`，确认页面加载且无 console 错误。

- [ ] **Step 4: 提交**

```bash
git add omni_desk_frontend/src/routes/index.jsx omni_desk_frontend/src/AdminAppWrapper.jsx
git commit -m "feat(joint-students-frontend): 注册 12 个路由与导航菜单项"
```

---

## Task 14: 集成 E2E 流程测试

**Files:**
- Create: `omni_desk_backend/joint_students/tests/test_e2e_flow.py`

**目的**: 覆盖从创建联培生到锁定补助的完整流程（后端集成测试）。

- [ ] **Step 1: 写 E2E 测试**

文件 `omni_desk_backend/joint_students/tests/test_e2e_flow.py`：

```python
"""联培生模块完整流程 E2E 测试 (后端集成)。"""
from datetime import date
from decimal import Decimal

import pytest
from rest_framework.test import APIClient

from joint_students.models import AssessmentCycle, StipendRecord
from joint_students.services.cycle import create_cycle
from joint_students.tests.factories import (
    create_group,
    create_joint_student,
    create_personnel,
    create_report,
    create_score,
    create_user,
)


@pytest.mark.django_db
class TestJointStudentEndToEndFlow:
    def test_full_flow_create_submit_review_score_close_lock(self):
        """完整流程:
        1. 联培生管理员创建 JointStudent
        2. 联培生提交月度报告
        3. 管理员审核通过
        4. 管理员手动触发批次
        5. 专家打分
        6. 关闭批次 (close_cycle 算 grade+stipend)
        7. 管理员锁定补助
        8. 联培生看到已锁定补助
        """
        # 准备用户
        manager = create_user(username="manager1")
        create_group("联培生管理员", manager)
        expert1 = create_user(username="expert1")
        create_group("考核专家组", expert1)
        expert2 = create_user(username="expert2")
        create_group("考核专家组", expert2)
        student_user = create_user(username="student1")

        # 准备联培生
        personnel = create_personnel(name="张三")
        personnel.user_account = student_user  # OneToOne reverse
        personnel.save()

        client = APIClient()

        # Step 1: 创建联培生
        client.force_authenticate(manager)
        resp = client.post("/api/joint-students/students/", {
            "personnel": personnel.id,
            "student_type": "master",
            "student_id": "S2026001",
            "enrollment_date": "2024-09-01",
        }, format="json")
        assert resp.status_code == 201, resp.data
        js_id = resp.data["id"]

        # Step 2: 联培生提交月度报告
        client.force_authenticate(student_user)
        resp = client.post("/api/joint-students/reports/", {
            "joint_student": js_id, "year": 2026, "month": 7,
            "work_progress": "本月工作", "work_highlights": "亮点",
            "attendance_days_actual": "22.0", "attendance_days_expected": "22.0",
        }, format="json")
        assert resp.status_code == 201
        report_id = resp.data["id"]

        resp = client.post(f"/api/joint-students/reports/{report_id}/submit/")
        assert resp.status_code == 200

        # Step 3: 管理员审核通过
        client.force_authenticate(manager)
        resp = client.post(f"/api/joint-students/reports/{report_id}/approve/")
        assert resp.status_code == 200

        # Step 4: 手动触发批次
        resp = client.post("/api/joint-students/cycles/trigger/", {
            "year": 2026, "month": 7,
        }, format="json")
        assert resp.status_code == 201
        cycle_id = resp.data["id"]

        # Step 5: 两位专家打分
        for expert in [expert1, expert2]:
            client.force_authenticate(expert)
            resp = client.post("/api/joint-students/scores/", {
                "cycle": cycle_id, "joint_student": js_id, "score": "95.00",
                "comment": "good",
            }, format="json")
            assert resp.status_code == 201

        # Step 6: 关闭批次 (使用 service 函数)
        cycle = AssessmentCycle.objects.get(id=cycle_id)
        from joint_students.services.cycle import close_cycle
        close_cycle(cycle)

        # 验证 StipendRecord 已创建 (status=pending)
        stipend = StipendRecord.objects.get(cycle=cycle, joint_student_id=js_id)
        assert stipend.status == "pending"
        assert stipend.average_score == Decimal("95.00")
        assert stipend.grade in ("A", "B")
        assert stipend.base_amount == Decimal("3000.00")

        # Step 7: 管理员锁定补助
        client.force_authenticate(manager)
        resp = client.post(f"/api/joint-students/stipends/{stipend.id}/lock/")
        assert resp.status_code == 200
        stipend.refresh_from_db()
        assert stipend.status == "locked"

        # Step 8: 联培生看到已锁定补助
        client.force_authenticate(student_user)
        resp = client.get("/api/joint-students/stipends/")
        assert resp.status_code == 200
        assert any(s["id"] == stipend.id for s in resp.data["results"]) if "results" in resp.data \
            else any(s["id"] == stipend.id for s in resp.data)
```

- [ ] **Step 2: 跑 E2E 测试**

```bash
cd omni_desk_backend && pytest --ds=omni_desk_backend.settings.test omni_desk_backend/joint_students/tests/test_e2e_flow.py -v
```

预期：PASS。

- [ ] **Step 3: 提交**

```bash
git add omni_desk_backend/joint_students/tests/test_e2e_flow.py
git commit -m "test(joint-students): 添加完整流程 E2E 测试"
```

---

## Task 15: 文档归档 (technical + user-manual + 删除本计划)

**Files:**
- Create: `docs/technical/37-joint-students-module.md`
- Create: `docs/user-manual/07-joint-students.md`
- Modify: `docs/technical/README.md`
- Modify: `docs/user-manual/README.md`
- Delete: `docs/superpowers/specs/2026-07-28-联培生管理模块-design.md` (设计已并入 technical)
- Delete: `docs/superpowers/plans/2026-07-28-联培生管理模块.md` (本文档)

- [ ] **Step 1: 创建技术文档章节**

文件 `docs/technical/37-joint-students-module.md`：

```markdown
# 联培生管理模块

## 概述

为 OmniDesk 添加联培生 (联合培养硕博研究生) 管理子模块。1:1 扩展 personnel.Personnel 主表,
复用 users / permissions / notifications 模块, 通过 Celery Beat 自动触发月末考核批次,
联培生管理员可手动提前触发。

## 数据模型

6 张表: JointStudent / MonthlyReport / AssessmentCycle / ExpertScore / StipendRecord。
详见 `docs/superpowers/specs/2026-07-28-联培生管理模块-design.md` (设计文档)。

## 业务规则

### A 档名额优先

按分数降序, 前 40% (向下取整) 为 A 档, 其余 B 档。
< 3 人全部 B 档。同分按 joint_student.id 升序稳定排序。

### 补助公式

`final_amount = base_amount × grade_coefficient × attendance_ratio`

- base_amount: 硕士 3000 / 博士 6000
- grade_coefficient: A=1.00, B=0.80
- attendance_ratio: min(actual/expected, 1.0), 应出勤 0 → 1.0

### 批次状态机

`collecting → closed (自动算 grade+stipend, status=pending) → finalized`
`StipendRecord: pending → locked (联培生管理员复核, 联培生可见)`

## 触发机制

- **自动**: Celery Beat 每月 JOINT_STUDENT_CYCLE_DAY 号 02:00 运行
- **手动**: 联培生管理员点 "提前发起本月考核" 按钮, 调用同一 Celery 任务
- **幂等**: unique_together(year, month) 兜底

## API

挂在 `/api/joint-students/` 下, DRF DefaultRouter:

- `/students/` - 联培生 CRUD
- `/reports/` - 月度报告 CRUD + submit/approve/reject actions
- `/cycles/` - 考核批次 + trigger/force_close actions
- `/scores/` - 专家打分 (独立, 互相不可见)
- `/stipends/` - 补助 (只读 + lock action)

## 权限

- 联培生管理员 (独立 Group, 与人员 HR 互不重叠)
- 考核专家组 (成员由 admin 手动添加)
- 联培生 (自己数据)
- 导师 (只读名下联培生)

详细权限矩阵见设计文档 §"权限模型"。

## 测试

`pytest --ds=omni_desk_backend.settings.test omni_desk_backend/joint_students/`

- test_models.py: 5 类模型约束
- test_grading.py: A 档算法 7 个边界 case
- test_stipend.py: 补助公式 11 个 case
- test_cycle.py: 批次生命周期
- test_tasks.py: Celery 任务
- test_api.py: API 权限矩阵
- test_e2e_flow.py: 完整流程

## 相关文件

- `omni_desk_backend/joint_students/` - 后端
- `omni_desk_frontend/src/features/joint-students/` - 前端
- 设计文档: `docs/superpowers/specs/2026-07-28-联培生管理模块-design.md`
```

- [ ] **Step 2: 创建用户手册章节**

文件 `docs/user-manual/07-joint-students.md`：

```markdown
# 联培生管理 - 用户手册

## 角色说明

| 角色 | 谁能做 |
|---|---|
| 联培生管理员 | 创建联培生 / 审核月度报告 / 触发考核批次 / 复核补助 |
| 考核专家 | 给联培生打分 |
| 联培生 | 填报月度报告 / 查看自己的补助 |
| 导师 | 查看名下联培生的月度报告 |

## 联培生管理员操作流程

### 创建联培生

1. 进入 "联培生管理" → "联培生列表" → "新建联培生"
2. 选择 Personnel (姓名)、培养类型 (硕士/博士)、学号、入学日期
3. 提交

### 审核月度报告

1. 进入 "联培生管理" → "报告审核"
2. 选择月份, 查看待审核报告
3. 点 "通过" 或 "驳回" (驳回需填写审核意见)

### 手动发起考核批次

1. 进入 "联培生管理" → "批次管理" → "提前发起本月考核"
2. 系统创建 AssessmentCycle 并自动通知专家组

### 复核补助

1. 进入 "联培生管理" → "补助复核"
2. 查看本月补助明细
3. 确认无误 → 点 "复核锁定"
4. 联培生即可看到补助

## 专家组操作流程

### 打分

1. 收到站内通知 "考核批次已发起"
2. 进入 "专家打分" 页面
3. 选择批次, 对每位联培生打分 (0-100)
4. 提交后分数自动锁定

## 联培生操作流程

### 填报月度报告

1. 进入 "我的月度报告" → "填报本月报告"
2. 填写工作进展、工作亮点、实际出勤天数
3. 提交 → 等待管理员审核

### 查看补助

1. 进入 "我的补助"
2. 仅显示 "已锁定" 状态的补助

## 常见问题

**Q: A 档名额硬限制 40%, 我得了 95 分但排第 6 (10 人), 我是什么档?**
A: 10 人 → 4 A 6 B, 第 5 名 (即使 90+ 分) 都是 B 档。

**Q: 应出勤 22 天, 我出勤 25 天 (加班), 出勤比怎么算?**
A: 出勤比上限 1.0, 不会因加班而 > 1。

**Q: 我打错分了, 能改吗?**
A: 不能自助改。请联系 admin 解锁 (admin 后台有 "解锁分数" 操作)。
```

- [ ] **Step 3: 更新 `docs/technical/README.md` 章节列表**

在表格追加一行：

```markdown
| 37 | [联培生管理模块](37-joint-students-module.md) | 联培生档案、月度报告、考核批次、补助计算 |
```

- [ ] **Step 4: 更新 `docs/user-manual/README.md` 章节列表**

在表格追加一行：

```markdown
| 07 | [联培生管理](07-joint-students.md) | 联培生月度报告 / 专家组打分 / 补助复核的操作流程 |
```

- [ ] **Step 5: 提交所有变更**

```bash
git add docs/technical/37-joint-students-module.md docs/user-manual/07-joint-students.md docs/technical/README.md docs/user-manual/README.md
git commit -m "docs(joint-students): 添加技术章节与用户手册"
```

- [ ] **Step 6: 删除已完成的设计文档与计划文档**

```bash
git rm docs/superpowers/specs/2026-07-28-联培生管理模块-design.md
git rm docs/superpowers/plans/2026-07-28-联培生管理模块.md
git commit -m "chore(docs): 联培生模块设计文档与计划已归档, 删除"
```

- [ ] **Step 7: 验证完整项目**

```bash
cd omni_desk_backend && pytest --ds=omni_desk_backend.settings.test omni_desk_backend/joint_students/
cd omni_desk_frontend && npm test -- --testPathPattern=joint-students
```

预期：所有后端 + 前端测试 PASS。

---

## Self-Review Checklist

实施者执行完所有 task 后，自查：

- [ ] 所有 16 个 task 的 `[ ]` 都变为 `[x]`
- [ ] `omni_desk_backend/joint_students/migrations/` 至少 3 个 migration 文件
- [ ] `pytest omni_desk_backend/joint_students/` 全部通过
- [ ] `npm test -- --testPathPattern=joint-students` 全部通过
- [ ] `python manage.py check` 无错误
- [ ] 浏览器访问 `http://localhost:3000/joint-students/admin/students` 页面正常加载
- [ ] E2E 流程测试通过（创建联培生 → 提交报告 → 审核 → 触发批次 → 打分 → 关闭 → 锁定 → 联培生可见）
- [ ] 设计文档与计划文档已删除（CLAUDE.md "plans 仅保留进行中"）
- [ ] CHANGELOG.md 更新（feat: 添加联培生管理模块）

---

## Notes for Implementer

- 任何 task 中遇到 `ImportError` / 字段名不匹配，**不要猜**，先看设计文档原文 (`docs/superpowers/specs/2026-07-28-联培生管理模块-design.md`)
- 写代码时**严格遵循 CLAUDE.md 全局规范**（语言用中文、Personnel 不改、DRF ModelViewSet 风格）
- **不要**修改 personnel 主表、notifications 模型本体、permissions 模型本体
- **不要**在 Cycle / Report / Stipend 上加未在设计文档中出现的字段
- 货币始终用 `Decimal`，不用 `float`
- 每次 commit 后**立即跑相关测试**，失败则**立即停止**该 task
- 任何 task 卡住超过 30 分钟 → 在 commit message 里写清楚卡点，向用户求助

---

---

---