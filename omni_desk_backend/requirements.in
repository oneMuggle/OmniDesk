# Core Frameworks
Django>=4.2.30,<5.0
djangorestframework==3.15.2
gunicorn>=23.0.0

# Authentication
djangorestframework-simplejwt==5.5.1

# JWT (transitive of simplejwt; pinned to avoid PYSEC-2026-175/177/178/179)
PyJWT==2.13.0

# Database & Cache
psycopg2-binary
redis
django-redis

# Async Tasks
celery
django-celery-beat

# Django Apps & Extensions
django-cors-headers
django-filter
django-extensions
django-ckeditor
drf-nested-routers
django-crum
django-js-asset
django-timezone-field

# Document & Data Handling
pypdf
python-docx
docxcompose
docxtpl
PyYAML
Markdown
beautifulsoup4
lxml
pillow
python-magic>=0.4.27  # MIME type validation (file_processing.views 依赖)
mammoth>=1.6,<2.0  # 旧 .doc 文档解析
pdfplumber>=0.10,<1.0  # PDF 文本提取 (processors/pdf.py)
openpyxl>=3.1,<4.0  # Excel 文件解析 (processors/excel.py)
pandas>=2.0,<3.0  # 数据处理 (与 tabulate 配套用于 to_markdown)
tabulate>=0.9.0  # pandas to_markdown 依赖

# Utilities
python-dotenv
requests
whitenoise
holidays
python-crontab
cron-descriptor
Unidecode
waitress

# AI & LLM (file_processing.ai.query 依赖)
ollama>=0.4,<1.0
anyio>=3.0  # 显式声明:ollama 0.6+ 的 _client.py import anyio 但 wheel 未声明硬依赖
httpx>=0.24,<0.28  # 显式 pin:ollama 0.6.2 假设 httpx 0.x 稳定版 API,新 httpx 1.0+ 已移除 base_url 关键字

# Rate Limiting
django-ratelimit

# Structured JSON logging (production)
python-json-logger==2.0.7